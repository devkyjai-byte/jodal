---
phase: 01-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/업종-물품분류-매핑.md
autonomous: true
requirements: [SC-2]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC-2: 업종코드 ↔ 조달 분류체계 매핑 설계 문서가 존재한다"
    - "SC-2: 나라장터의 서로 다른 코드 체계(NTS 업태·종목 / G2B 업종코드 / 물품분류번호)가 표로 구분되어 있어 혼동이 불가능하다"
    - "SC-2: 목표 업종 3~5개 각각에 실제 물품분류번호(또는 대분류·중분류 prefix)가 매핑되어 있다 (D-08)"
    - "SC-2: 업체 프로필이 저장할 값이 물품분류번호 계열임이 명시되어 있다 (D-06)"
    - "KSIC 이중 매핑 테이블은 이 문서에 설계되지 않는다 (D-07)"
    - "재검증하지 못한 코드는 [미확인] 표기와 확인 방법이 함께 남아 있어 Phase 2 가 잘못된 코드를 사실로 오인하지 않는다"
  artifacts:
    - docs/design/업종-물품분류-매핑.md
  key_links:
    - "매핑 문서의 프로필 저장 형식 결정 → 플랜 03 `company_classification_codes` 테이블의 컬럼 정의"
    - "매핑 문서의 prefix 매칭 규칙 → Phase 2 매칭 스코어링 엔진(MATCH-01)의 1차 필터 조건"
    - "매핑 문서의 대분류/중분류 선택 UI 권장안 → 플랜 04 온보딩 화면의 업종 선택 컴포넌트"
---

<objective>
업체 프로필의 "업종"과 나라장터 공고의 분류체계를 무엇으로 조인할지 확정하고, 목표 업종 3~5개에 대한 실제 코드 매핑표를 설계 문서로 남긴다.

Purpose: RESEARCH.md 가 식별한 이 Phase 최대의 설계 함정은 이름이 비슷한 3개 코드 체계(국세청 업태·종목 / 나라장터 참가자격 업종코드 / UNSPSC 기반 물품분류번호)의 혼동이다. 여기서 잘못 결정하면 Phase 2 의 매칭 엔진(MATCH-01)이 조인할 키를 잃어 전면 재설계가 필요하다. D-06 결정(물품분류번호를 1차 기준)을 문서 수준에서 확정하고, RESEARCH.md Open Question 1 의 `[ASSUMED]` 코드들을 가능한 만큼 실제 조회로 대체한다.
Output: `docs/design/업종-물품분류-매핑.md`

**이 Phase 는 애플리케이션 코드를 생성하지 않는다.** 매핑 데이터를 적재하는 시드 스크립트나 CSV 로더를 만들지 않는다 — 매핑표는 마크다운 문서로만 남기고, 실제 데이터 적재는 Phase 2 의 몫이다 (CONTEXT.md D-04/D-05/D-09).

**Edge Coverage / Prohibitions 프로빙 생략 (VISIBLE SKIP):** Phase 1 은 REQ-ID 기반 기능 요구사항이 없는 설정 단계다 (ROADMAP.md `Requirements: (설정 단계 — 기능 요구사항 없음)`). 프로빙할 요구사항 텍스트가 없어 edge-probe 엔진을 실행하지 않았고, must_haves 는 ROADMAP.md Phase 1 Success Criteria 2번(SC-2)에서 역산했다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-prep/01-CONTEXT.md
@.planning/phases/01-prep/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 코드 체계 구분표 + 목표 업종 물품분류번호 실조회·확정</name>
  <files>docs/design/업종-물품분류-매핑.md</files>
  <read_first>
    - `.planning/phases/01-prep/01-RESEARCH.md` — 특히 §"나라장터 두 코드 체계 — 반드시 구분"(업종코드 vs 물품분류번호 비교표, 등록/조회처 URL), §"UNSPSC 대분류(Segment) 구조 및 목표 업종 후보"(43/44/72/76 후보와 `[ASSUMED]` 표기), §"Common Pitfalls > Pitfall 1"(코드 혼동), §"Open Questions" 1번, §"Assumptions Log" A1·A2
    - `.planning/phases/01-prep/01-CONTEXT.md` §decisions > 업종코드 매핑 기준 — D-06, D-07, D-08
    - `.planning/REQUIREMENTS.md` — PROF-01 원문("업체는 업종코드(업태·품목)를 등록할 수 있다")과 MATCH-01
    - `.planning/ROADMAP.md` §Phase 1 Success Criteria 2번
  </read_first>
  <precondition>인터넷 조회가 가능해야 한다 — 목표 업종의 실제 물품분류번호를 나라장터 목록정보사이트(goods.g2b.go.kr) 또는 웹 검색으로 확인해야 하며, 조회가 불가능하면 RESEARCH.md 의 `[ASSUMED]` 코드를 그대로 옮기지 말고 `[미확인]` 으로 표기하고 확인 방법을 남긴 뒤 진행한다.</precondition>
  <action>
`docs/design/업종-물품분류-매핑.md` 를 생성하고 다음 4개 섹션을 이 순서로 작성한다.

1. `## 결론 먼저` — 3~5줄. 업체 프로필이 저장하고 공고와 조인할 값은 **나라장터 물품분류번호(UNSPSC 기반 8자리) 계열**이며, 국세청 사업자등록증의 업태·종목이나 나라장터 참가자격 업종코드는 조인 키로 쓰지 않는다는 결론을 D-06 근거와 함께 단정적으로 서술한다. PROF-01 요구사항 문구에는 "업종코드"라고 적혀 있으나 실제 저장 값은 물품분류번호 계열이라는 용어 불일치를 Phase 2 planner 가 반드시 인지해야 한다고 명시한다.

2. `## 세 가지 코드 체계 구분` — 마크다운 표. 컬럼: 체계명 / 목적 / 적용 대상 / 형식·자릿수 / 조회처 / 이 프로젝트에서의 사용 여부. 3행:
   - 국세청 업태·종목 — 세무 목적의 사업 분류 / 사업자 / 텍스트 명칭 / 사업자등록증·홈택스 / **사용 안 함(조인 불가)**
   - 나라장터 업종코드(참가자격) — 업체가 어떤 업종으로 입찰 참여 가능한지 식별 / 공급자(업체) / 4자리류 (예: 1468=소프트웨어사업, 1426=패키지소프트웨어개발·공급, 0037=전기공사업) / 나의나라장터 > 업체정보관리 > 입찰참가자격변경신청, data.g2b.go.kr / **참고용 보조 필드(향후 자격판별 Phase 4 에서 재검토)**
   - 물품분류번호(분류코드) — 조달 물품·용역·공사 자체의 성격 분류 / 공고·계약 대상 품목 / UNSPSC 기반 8자리 = 2자리×4단계(대분류 Segment - 중분류 Family - 소분류 Class - 품명분류 Commodity) / 목록정보사이트 goods.g2b.go.kr:8053 / **1차 조인 키 (D-06)**
   표 아래에 RESEARCH.md 출처 신뢰도를 한 줄로 밝힌다 — 이 구분은 MEDIUM confidence(개인 블로그 + 웹검색 교차확인)이며 공식 원문 미확인이므로 Phase 2 착수 시 실제 API 응답 필드로 재확인한다.

3. `## 목표 업종 매핑표` — D-08 에 따라 목표 업종을 정확히 4개로 시작한다: 정보통신·소프트웨어개발 / 사무용품 / 시설관리 / 인쇄·출판. 각 업종에 대해 웹 조회(goods.g2b.go.kr 목록정보사이트 검색, 또는 접근 불가 시 웹 검색)로 실제 코드를 확인하고 아래 컬럼의 표를 채운다: 목표 업종 / 대분류(2자리) / 대분류명 / 대표 중분류·소분류 예시 / 대표 8자리 품명분류 예시 / 검증 상태 / 근거.
   - 검증 상태 컬럼에는 반드시 `[확인]` 또는 `[미확인]` 중 하나를 기입한다. 실제 조회로 확인한 코드만 `[확인]`, RESEARCH.md 의 추정치를 옮긴 것은 `[미확인]` 이다. **RESEARCH.md 의 `[ASSUMED]` 값을 검증 없이 `[확인]` 으로 승격하지 않는다.**
   - 근거 컬럼에는 조회한 URL 또는 "RESEARCH.md A1/A2 추정" 을 적는다.
   - 용역 계열(시설관리)이 물품분류와 다른 체계(용역분류)를 쓰는지 여부를 확인하고, 확인되면 표 아래 각주로 별도 서술한다 (RESEARCH.md Assumption A2).

4. `## 미확인 항목과 확인 방법` — 위 표에서 `[미확인]` 으로 남은 모든 행을 다시 나열하고, 각각에 대해 (a) 정확한 조회 경로(goods.g2b.go.kr:8053 검색 화면에서 어떤 키워드로 검색), (b) 누가 언제 확인하는가, (c) 틀렸을 때의 영향(해당 업종 매칭이 처음부터 어긋남)을 적는다. RESEARCH.md Assumptions Log A1·A2 를 인용한다.

문서 최상단에 `> 이 문서는 설계 참고 문서이며 실행 코드가 아니다.` 한 줄을 둔다. 매핑 데이터를 DB 에 넣는 SQL·CSV·시드 스크립트 파일은 생성하지 않는다.
  </action>
  <reversibility rating="costly">"물품분류번호를 1차 조인 키로 삼는다"(D-06)는 결정이 Phase 2 의 프로필 스키마·온보딩 UI·매칭 엔진 전부의 전제가 된다. 뒤집으려면 세 곳을 동시에 고쳐야 한다. 다만 CONTEXT.md 에서 이미 확정된 결정을 문서화하는 것이므로 이 태스크가 새로 지는 위험은 아니다.</reversibility>
  <verify>
    <automated>test -f "docs/design/업종-물품분류-매핑.md" && for sec in "결론 먼저" "세 가지 코드 체계 구분" "목표 업종 매핑표" "미확인 항목과 확인 방법"; do grep -q "## $sec" "docs/design/업종-물품분류-매핑.md" || { echo "FAIL: section $sec missing"; exit 1; }; done && grep -q "D-06" "docs/design/업종-물품분류-매핑.md" && grep -q "D-08" "docs/design/업종-물품분류-매핑.md" && grep -q "UNSPSC" "docs/design/업종-물품분류-매핑.md" && grep -q "goods.g2b.go.kr" "docs/design/업종-물품분류-매핑.md" && test $(grep -cE '\[확인\]|\[미확인\]' "docs/design/업종-물품분류-매핑.md") -ge 4 && test $(grep -oE '\b[0-9]{8}\b' "docs/design/업종-물품분류-매핑.md" | wc -l) -ge 4 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/design/업종-물품분류-매핑.md` 파일이 존재한다
    - `## 결론 먼저`, `## 세 가지 코드 체계 구분`, `## 목표 업종 매핑표`, `## 미확인 항목과 확인 방법` 4개 h2 섹션이 모두 존재한다
    - 코드 체계 구분표에 국세청 업태·종목 / 나라장터 업종코드 / 물품분류번호 3개 체계가 각각 별도 행으로 있고, 각 행에 형식·자릿수와 조회처가 채워져 있다
    - 목표 업종 매핑표에 목표 업종이 정확히 4개 행으로 존재한다 (D-08 의 3~5개 범위 내)
    - 매핑표의 모든 행에 `[확인]` 또는 `[미확인]` 검증 상태가 기입되어 있다 (합계 4개 이상)
    - 8자리 숫자 코드가 4개 이상 문서에 등장한다
    - `D-06`, `D-08` 이 근거로 인용되어 있고 `UNSPSC` 와 `goods.g2b.go.kr` 이 문서에 등장한다
    - `docs/design/` 아래에 `.sql`, `.csv`, `.ts`, `.py` 확장자 파일이 생성되지 않았다
  </acceptance_criteria>
  <done>Phase 2 planner 가 이 문서 한 장만 읽고도 "업체 프로필에 무엇을 저장하고 공고의 어느 필드와 조인하는가"를 오해 없이 알 수 있으며, 아직 검증되지 않은 코드가 무엇인지 명시적으로 구분된다.</done>
</task>

<task type="auto">
  <name>Task 2: 매칭 운용 규칙과 범위 경계 서술 추가</name>
  <files>docs/design/업종-물품분류-매핑.md</files>
  <read_first>
    - `docs/design/업종-물품분류-매핑.md` — Task 1 이 작성한 앞선 4개 섹션 (이어서 덧붙인다)
    - `.planning/phases/01-prep/01-CONTEXT.md` §decisions — D-06, D-07, D-08 및 §deferred(KSIC 이중 매핑 제외)
    - `.planning/phases/01-prep/01-RESEARCH.md` §"나라장터 두 코드 체계"의 설계 함의 문단(대분류/중분류 다중 선택 UI 권장), §"Common Pitfalls > Pitfall 1" Warning signs
    - `.planning/REQUIREMENTS.md` — MATCH-01(스코어링 매칭), PROF-01
  </read_first>
  <action>
Task 1 이 만든 `docs/design/업종-물품분류-매핑.md` 의 끝에 다음 3개 섹션을 이어서 추가한다 (기존 4개 섹션은 수정하지 않는다).

5. `## 프로필 저장 형식 권장안` — 업체 프로필이 물품분류번호를 어떤 입도로 저장할지 결정하고 근거를 적는다. 권장안: 업체는 온보딩에서 **대분류(2자리) 또는 중분류(4자리) 단위를 다중 선택**하고, 저장 시에는 선택한 prefix 문자열을 그대로 보관한다. 8자리 품명분류 단위 강제 입력은 요구하지 않는다 — 이유: 중소 업체가 자사 취급 품목의 8자리 코드를 모두 알기 어렵고, 너무 좁게 잡으면 매칭 리콜이 급감한다. 저장 값의 자릿수가 2/4/6/8 로 가변임을 명시하고, 이것이 플랜 03 의 컬럼 정의에 미치는 영향(고정 8자리 컬럼이 아니라 가변 길이 + 자릿수 구분 필요)을 한 문장으로 남긴다.

6. `## 매칭 규칙 (Phase 2 인계)` — 공고의 분류코드와 업체 저장 prefix 를 어떻게 대조할지 규칙을 서술한다. 최소 다음을 포함한다: (a) 기본 규칙은 prefix 일치 — 공고 분류코드가 업체가 등록한 prefix 로 시작하면 후보로 삼는다, (b) 일치 자릿수가 길수록(8 > 6 > 4 > 2) 적합도 점수를 높게 준다는 방향성, (c) 분류코드가 비어 있는 공고의 처리 방침(제외하지 않고 제목·본문 키워드 매칭으로 보조 판정 후보에 남긴다), (d) 지역·실적·인증은 분류코드와 별개의 가중치 축이며 이 문서의 범위가 아니라는 경계. 구체적 점수 수식은 확정하지 않고 "Phase 2 에서 확정" 이라고 명시한다 — 이 문서는 조인 키 설계 문서이지 스코어링 알고리즘 설계 문서가 아니다.

7. `## 이번 Phase 범위 밖` — 다음 3가지를 각각 근거 결정 ID 와 함께 명시적으로 제외 선언한다: (a) 통계청 표준산업분류(KSIC) ↔ 물품분류번호 이중 매핑 테이블 — D-07 에 따라 이번 Phase 에서 만들지 않으며 필요해지면 이후 Phase 에서 추가, (b) 목표 4개 업종을 넘는 전체 분류체계 매핑 — D-08 에 따라 초기 사용자 확보에 집중, (c) 나라장터 참가자격 업종코드 기반 자격 판별 — Phase 4(ELIG) 의 범위.

문서 최하단에 `---` 구분선과 함께 `*Phase 1 산출물 · 작성일: {YYYY-MM-DD}*` 한 줄을 남긴다.
  </action>
  <reversibility rating="reversible">문서에 추가하는 권장안·규칙 서술이며 Phase 2 착수 전 언제든 갱신 가능하다.</reversibility>
  <verify>
    <automated>for sec in "프로필 저장 형식 권장안" "매칭 규칙 (Phase 2 인계)" "이번 Phase 범위 밖"; do grep -q "## $sec" "docs/design/업종-물품분류-매핑.md" || { echo "FAIL: section $sec missing"; exit 1; }; done && grep -q "D-07" "docs/design/업종-물품분류-매핑.md" && grep -q "prefix" "docs/design/업종-물품분류-매핑.md" && test $(grep -c '^## ' "docs/design/업종-물품분류-매핑.md") -eq 7 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/design/업종-물품분류-매핑.md` 의 h2 섹션이 정확히 7개다 (Task 1 의 4개 + Task 2 의 3개, 초과 섹션 없음)
    - `## 프로필 저장 형식 권장안`, `## 매칭 규칙 (Phase 2 인계)`, `## 이번 Phase 범위 밖` 3개 섹션이 존재한다
    - 프로필 저장 형식 권장안에 저장 값 자릿수가 가변(2/4/6/8)임과 플랜 03 컬럼 정의에 미치는 영향이 서술되어 있다
    - 매칭 규칙에 prefix 일치 방식과, 구체적 점수 수식은 Phase 2 에서 확정한다는 경계가 명시되어 있다
    - `## 이번 Phase 범위 밖` 에 KSIC 제외가 `D-07` 인용과 함께 명시되어 있다
    - Task 1 이 작성한 4개 섹션의 내용이 변경되지 않았다
  </acceptance_criteria>
  <done>매핑 문서가 "무엇을 저장하고 어떻게 대조하며 무엇은 하지 않는가"까지 답하는 완결된 인계 문서가 되어, Phase 2 planner 가 추가 질문 없이 프로필 스키마와 매칭 1차 필터를 설계할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 웹(goods.g2b.go.kr, 검색엔진) → 설계 문서 | 조회로 얻은 코드값이 검증 없이 문서에 사실로 기록되면 Phase 2 전체가 잘못된 전제 위에 세워진다. |
| 이 설계 문서 → Phase 2 구현 | 이 문서의 결정이 코드 검증 없이 그대로 스키마·매칭 엔진에 반영된다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | `docs/design/업종-물품분류-매핑.md` 의 물품분류번호 값 | high | mitigate | 매핑표에 `[확인]`/`[미확인]` 검증 상태 컬럼을 필수화하고, RESEARCH.md 의 `[ASSUMED]` 값을 검증 없이 `[확인]` 으로 승격하는 것을 금지. `## 미확인 항목과 확인 방법` 섹션에 재검증 경로를 남겨 Phase 2 가 무검증 코드를 사실로 오인하지 않게 한다. |
| T-01-07 | Tampering | 코드 체계 혼동으로 인한 조인 키 오설계 | high | mitigate | 3개 체계 구분표를 문서 앞부분에 배치하고, 각 체계의 "이 프로젝트에서의 사용 여부" 컬럼에 사용/미사용을 단정적으로 기재 (RESEARCH.md Pitfall 1). |
| T-01-08 | Information Disclosure | 조회 과정에서 개인 계정 정보 노출 | low | accept | goods.g2b.go.kr 목록정보 검색은 공개 조회 기능이며 로그인 자격증명을 문서에 남기지 않는다. 로그인이 필요한 화면은 조회하지 않고 `[미확인]` 처리한다. |
| T-01-09 | Repudiation | 매핑 코드의 출처 불명 | medium | mitigate | 매핑표 근거 컬럼에 조회 URL 또는 `RESEARCH.md A1/A2 추정` 출처를 필수 기재해 각 값의 신뢰 수준을 추적 가능하게 한다. |
| T-01-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Phase 에는 패키지 매니저 설치 태스크가 전혀 없다 (RESEARCH.md §Package Legitimacy Audit: "해당 없음"). Phase 2 착수 시 Package Legitimacy Gate 를 적용한다. |
</threat_model>

<verification>
1. `docs/design/업종-물품분류-매핑.md` 이 존재하고 h2 섹션이 정확히 7개다.
2. 각 태스크의 `<automated>` 게이트가 PASS 를 출력한다.
3. 매핑표의 모든 행에 검증 상태(`[확인]`/`[미확인]`)와 근거가 있다.
4. `docs/design/` 아래에 애플리케이션 소스코드·SQL·CSV 파일이 생성되지 않았다 — 이 Phase 는 마크다운 문서만 산출한다.
5. KSIC 이중 매핑 테이블이 설계되지 않았다 (D-07 준수).
</verification>

<success_criteria>
- SC-2 달성: 업종코드 ↔ 조달 분류체계 매핑 설계 문서가 존재하고 목표 업종 4개에 실제 코드가 매핑되어 있다
- 3개 코드 체계가 표로 구분되어 Phase 2 에서 혼동이 불가능하다 (RESEARCH.md Pitfall 1 차단)
- 검증되지 않은 코드가 `[미확인]` 으로 명시되어 잘못된 전제가 조용히 전파되지 않는다
- 프로필 저장 형식과 prefix 매칭 규칙이 플랜 03·04 와 Phase 2 로 인계 가능한 수준으로 결정되어 있다
</success_criteria>

<output>
Create `.planning/phases/01-prep/01-02-SUMMARY.md` when done
</output>
