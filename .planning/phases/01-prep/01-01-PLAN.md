---
phase: 01-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/api-신청-체크리스트.md
  - docs/design/도메인-서비스명-체크리스트.md
autonomous: true
requirements: [SC-1, SC-4]

user_setup:
  - service: data.go.kr (공공데이터포털)
    why: "나라장터 Open API 4종 활용신청 — 로그인·본인확인이 필요해 Claude가 대행 불가 (SC-1)"
    env_vars:
      - name: NARAJANGTEO_SERVICE_KEY
        source: "data.go.kr > 마이페이지 > API활용현황 > 일반 인증키(Encoding/Decoding) — 승인 후 발급. .env 에만 저장하고 git 에 커밋 금지"
    dashboard_config:
      - task: "조달청_나라장터 입찰공고정보서비스 활용신청 (data.go.kr/data/15129394/openapi.do)"
        location: "data.go.kr 서비스 상세페이지 > 활용신청"
      - task: "조달청_나라장터 사전규격정보서비스 활용신청 (data.go.kr/data/15129437/openapi.do)"
        location: "data.go.kr 서비스 상세페이지 > 활용신청"
      - task: "조달청_나라장터 낙찰정보서비스 활용신청 (data.go.kr/data/15129397/openapi.do)"
        location: "data.go.kr 서비스 상세페이지 > 활용신청"
      - task: "조달청_나라장터 계약정보서비스 활용신청 (data.go.kr/data/15129427/openapi.do)"
        location: "data.go.kr 서비스 상세페이지 > 활용신청"
  - service: 도메인 등록대행업체 (가비아 / 후이즈 등)
    why: "jodalmate.co.kr 도메인 등록 — 결제수단이 필요해 Claude가 대행 불가 (D-03, SC-4)"
    dashboard_config:
      - task: "jodalmate.co.kr 등록가능 여부 검색 후 직접 결제"
        location: "가비아 또는 후이즈 도메인 검색 화면"

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC-1: 나라장터 Open API 4종(입찰공고정보·사전규격정보·낙찰정보·계약정보)을 사용자가 순서대로 신청할 수 있는 실행 가능한 절차 문서가 존재한다"
    - "SC-1: 4개 서비스 각각의 승인방식(자동승인/심의승인)과 승인 상태를 기록할 수 있는 추적표가 존재한다 (RESEARCH.md Open Question 2 해소 경로)"
    - "SC-4: 서비스명이 조달메이트로 확정되어 문서에 기록되어 있다 (D-01)"
    - "SC-4: 도메인 후보가 jodalmate.co.kr 로 명시되고, 등록은 사용자가 직접 수행하는 체크리스트 항목으로 남아 있다 (D-02, D-03)"
    - "인증키(서비스키) 실제 값이 어떤 문서·git 산출물에도 기록되지 않는다"
  artifacts:
    - docs/design/api-신청-체크리스트.md
    - docs/design/도메인-서비스명-체크리스트.md
  key_links:
    - "api-신청-체크리스트.md 의 서비스키 보관 규칙 → Phase 2 Ingestion 배치의 환경변수 설정 (ING-01)"
    - "도메인-서비스명-체크리스트.md 의 확정 도메인 → Phase 2/3 배포 인프라 및 PWA manifest·스토어 등록 정보"
---

<objective>
Phase 1 의 두 가지 "사용자 액션 선행 과제"를 Claude 가 대신 준비할 수 있는 최대치까지 문서화한다: (1) 공공데이터포털 나라장터 Open API 4종 활용신청 절차·추적표, (2) 서비스명·도메인 확정 체크리스트.

Purpose: RESEARCH.md Pitfall 2 에 따라 API 승인 대기 시간이 Phase 1 전체의 임계 경로다. 이 플랜을 Wave 1 에 두어 사용자가 즉시 4건을 동시 제출하고, 승인 대기 중 나머지 설계 산출물(플랜 02~04)이 병렬로 진행되게 한다. 도메인은 결제가 필요해 D-03 대로 사용자 액션으로 남긴다.
Output: `docs/design/api-신청-체크리스트.md`, `docs/design/도메인-서비스명-체크리스트.md`

**이 Phase 는 애플리케이션 코드를 생성하지 않는다.** 소스코드·DB 마이그레이션·HTML 목업을 만들지 않는다 (CONTEXT.md D-04/D-05/D-09). 모든 산출물은 마크다운 설계·체크리스트 문서다.

**Edge Coverage / Prohibitions 프로빙 생략 (VISIBLE SKIP):** Phase 1 은 REQ-ID 기반 기능 요구사항이 없는 설정 단계다 (ROADMAP.md `Requirements: (설정 단계 — 기능 요구사항 없음)`, REQUIREMENTS.md 의 PROF/ING/MATCH/CLIENT/ELIG/BID/DOC 는 모두 Phase 2 이후 소속). 프로빙할 요구사항 텍스트 자체가 없으므로 edge-probe 엔진을 실행하지 않았다. 대신 must_haves 는 ROADMAP.md 의 Phase 1 Success Criteria 4개(SC-1~SC-4)에서 역산했다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prep/01-CONTEXT.md
@.planning/phases/01-prep/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 나라장터 Open API 4종 활용신청 체크리스트 작성</name>
  <files>docs/design/api-신청-체크리스트.md</files>
  <read_first>
    - `.planning/phases/01-prep/01-RESEARCH.md` — 특히 §"나라장터 Open API 4종 개요"(4개 서비스의 data.go.kr 데이터셋 ID 와 제공 정보), §"API 활용신청 절차"(5단계 절차, 자동승인/심의승인 구분, 개발계정 일 1,000건 → 운영계정 10만건 전환), §"Common Pitfalls > Pitfall 2"(승인 소요시간 과소평가), §"Open Questions" 2번·4번, §"Security Domain"(인증키 유출 위협)
    - `.planning/phases/01-prep/01-CONTEXT.md` §canonical_refs — 신청 대상 4개 서비스 목록
    - `.planning/ROADMAP.md` §Phase 1 Success Criteria 1번
    - `.claude/CLAUDE.md` — 민감정보 취급 제약
  </read_first>
  <action>
`docs/design/` 디렉터리를 새로 만들고 그 안에 `api-신청-체크리스트.md` 를 작성한다. 이 문서는 사용자가 브라우저를 열어놓고 그대로 따라 실행할 수 있는 운영 문서여야 하며, 다음 6개 섹션을 이 순서로 포함한다.

1. `## 왜 이것부터인가` — RESEARCH.md Pitfall 2 를 근거로, 4건을 동시에 제출해야 하는 이유(서비스별 승인방식이 자동/심의로 다를 수 있고 심의는 1~2일 소요 보고)와 승인 대기 중 플랜 02~04 가 병렬 진행된다는 점을 3~5줄로 서술.

2. `## 신청 대상 4종` — 마크다운 표. 컬럼: 서비스명 / data.go.kr URL / 데이터셋 ID / 이 프로젝트에서 쓰는 곳. 4행을 채운다:
   - 조달청_나라장터 입찰공고정보서비스 / https://www.data.go.kr/data/15129394/openapi.do / 15129394 / 공고 수집 본체(ING-01~03), Phase 2
   - 조달청_나라장터 사전규격정보서비스 / https://www.data.go.kr/data/15129437/openapi.do / 15129437 / 사전규격 단계 선행 탐지, Phase 2 이후
   - 조달청_나라장터 낙찰정보서비스 / https://www.data.go.kr/data/15129397/openapi.do / 15129397 / 낙찰률·경쟁강도 통계(BID-01~03), Phase 5
   - 조달청_나라장터 계약정보서비스 / https://www.data.go.kr/data/15129427/openapi.do / 15129427 / 계약 체결·변경 이력, Phase 5

3. `## 신청 절차` — 번호 매긴 단계. RESEARCH.md §"API 활용신청 절차"의 5단계를 그대로 반영하되 각 단계에 "사용자가 화면에서 무엇을 클릭/입력하는가"를 구체적으로 적는다. 활용목적 기재 예시 문구를 하나 제공한다 — 조달메이트의 성격(중소기업 대상 나라장터 공고 맞춤 매칭·알림 서비스, 정보 제공·보조 도구)을 반영하고, PROJECT.md 법적 제약에 따라 낙찰 보장·입찰 대행을 연상시키는 표현은 쓰지 않는다. 상세기능(오퍼레이션)은 우선 전체 선택 후 실사용 확정 시 조정하라고 안내한다.

4. `## 신청 상태 추적표` — 사용자가 신청 후 직접 채워 넣는 표. 컬럼: 서비스명 / 신청일 / 승인방식(자동승인·심의승인) / 승인일 / 상태(신청전·승인대기·승인·반려) / 비고. 4행 모두 상태 컬럼 초기값을 `신청전` 으로 채워 둔다. 표 아래에 "승인대기가 24시간을 넘으면 심의승인 대상이므로 data.go.kr QA 게시판 문의" 라는 경고 한 줄을 붙인다(RESEARCH.md Warning signs).

5. `## 서비스키 보관 규칙` — 승인 후 발급되는 인증키를 다루는 규칙. 다음을 명시한다: 키는 `.env` 파일의 `NARAJANGTEO_SERVICE_KEY` 로 보관한다 / `.env` 는 `.gitignore` 에 포함되어야 한다 / 이 문서를 포함한 어떤 마크다운 산출물에도 실제 키 값을 붙여넣지 않는다 / 키는 서버 사이드에서만 사용하고 브라우저(Next.js 클라이언트 번들)로 절대 내보내지 않는다 / Encoding 키와 Decoding 키 두 종류가 발급되므로 실제 호출 시 어느 쪽이 동작하는지 Phase 2 에서 확인한다. 예시 값 자리에는 반드시 `<REDACTED>` 같은 자리표시자만 쓴다.

6. `## 미해결 확인 항목` — RESEARCH.md Open Questions 2번·4번을 그대로 옮겨 적고, 각각 "누가 언제 확인하는가"를 붙인다: (a) 4개 서비스 각각의 승인방식 → 사용자가 신청 시 화면에서 확인해 위 추적표에 기록, (b) 개발계정 일 1,000건 한도가 ING-01(일 4~6회 폴링)에 충분한지 → Phase 2 실행 시 실제 페이지네이션 응답을 보고 판단, 운영계정 전환은 활용사례 등록 후 최대 10만건/일.

문서 최상단에는 `> 이 문서는 설계·운영 참고 문서이며 실행 코드가 아니다.` 한 줄을 둔다. 문서 어디에도 실제 인증키 값, 계정 비밀번호를 적지 않는다.
  </action>
  <reversibility rating="reversible">문서 산출물이며 언제든 재작성 가능. 다만 이 문서가 유도하는 사용자의 API 활용신청 제출 자체는 되돌리기 번거로우므로(반려 시 재신청 필요) 신청 문구 예시는 법적 제약을 반영해 신중히 작성한다.</reversibility>
  <verify>
    <automated>test -f "docs/design/api-신청-체크리스트.md" && for id in 15129394 15129437 15129397 15129427; do grep -q "$id" "docs/design/api-신청-체크리스트.md" || { echo "FAIL: dataset id $id missing"; exit 1; }; done && for sec in "왜 이것부터인가" "신청 대상 4종" "신청 절차" "신청 상태 추적표" "서비스키 보관 규칙" "미해결 확인 항목"; do grep -q "## $sec" "docs/design/api-신청-체크리스트.md" || { echo "FAIL: section $sec missing"; exit 1; }; done && grep -q "NARAJANGTEO_SERVICE_KEY" "docs/design/api-신청-체크리스트.md" && grep -q "gitignore" "docs/design/api-신청-체크리스트.md" && test $(grep -c "신청전" "docs/design/api-신청-체크리스트.md") -ge 4 && echo PASS</automated>
    <human-check>data.go.kr 에 로그인해 위 4개 URL 에서 활용신청을 실제로 제출하고, 각 서비스의 승인방식과 상태를 문서의 추적표에 기록한다. (end-of-phase 검증 시점에 확인)</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/design/api-신청-체크리스트.md` 파일이 존재한다
    - 데이터셋 ID `15129394`, `15129437`, `15129397`, `15129427` 4개가 모두 문서에 등장한다
    - `## 왜 이것부터인가`, `## 신청 대상 4종`, `## 신청 절차`, `## 신청 상태 추적표`, `## 서비스키 보관 규칙`, `## 미해결 확인 항목` 6개 h2 섹션이 모두 존재한다
    - 신청 상태 추적표에 4개 서비스 행이 있고 각 행의 상태 컬럼 초기값이 `신청전` 이다 (`신청전` 문자열이 4회 이상 등장)
    - 서비스키 보관 규칙에 `NARAJANGTEO_SERVICE_KEY` 와 `gitignore` 가 명시되어 있다
    - 문서에 실제 인증키 값 형태의 문자열(40자 이상 연속 영숫자/퍼센트인코딩 토큰)이 없다
    - 활용목적 예시 문구에 "낙찰 보장", "입찰 대행" 류의 금지 표현이 없다
  </acceptance_criteria>
  <done>사용자가 이 문서만 열어두고 data.go.kr 에서 4건의 활용신청을 순서대로 제출하고 승인 상태를 추적할 수 있으며, 발급받은 키를 어디에 어떻게 보관해야 하는지 문서에 규정되어 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 서비스명·도메인 확정 체크리스트 작성</name>
  <files>docs/design/도메인-서비스명-체크리스트.md</files>
  <read_first>
    - `.planning/phases/01-prep/01-CONTEXT.md` §decisions > 서비스명·도메인 — D-01(서비스명 조달메이트 확정), D-02(.co.kr 우선), D-03(도메인 구매는 사용자 직접 수행)
    - `.planning/phases/01-prep/01-RESEARCH.md` §"Common Pitfalls > Pitfall 3"(.co.kr 즉시 확보 가정 위험), §"Open Questions" 3번(co.kr 등록 자격 제한 여부), §"Environment Availability"(도메인 등록대행업체는 사용자 액션)
    - `.planning/ROADMAP.md` §Phase 1 Success Criteria 4번
    - `.planning/PROJECT.md` — 서비스명이 이미 사용된 문맥
  </read_first>
  <action>
`docs/design/도메인-서비스명-체크리스트.md` 를 작성한다. 다음 5개 섹션을 이 순서로 포함한다.

1. `## 서비스명 (확정)` — 서비스명은 `조달메이트` 로 확정됐음을 D-01 근거와 함께 기록한다. 확정 근거: PROJECT.md, ROADMAP.md, REQUIREMENTS.md 및 GitHub 저장소 설명에 이미 전면 사용 중이라 변경 비용이 크다. 영문 표기는 `Jodalmate` 로 병기하고, 이 표기가 도메인·스토어 등록·PWA manifest 의 `name`/`short_name` 에 공통으로 쓰일 기준값임을 명시한다. 이 항목은 추가 사용자 액션 없이 이미 완료 상태다.

2. `## 도메인 방침` — D-02 에 따라 `.co.kr` 을 1순위로 한다는 결정과 근거(국내 B2B·조달 업체 대상 서비스에서 `.co.kr` 이 신뢰도 면에서 유리)를 기록한다. 1순위 후보 `jodalmate.co.kr`, 2순위 대체 후보를 3개 이상 제시한다(예: `jodalmate.kr`, `jodal-mate.co.kr`, `jodalmate.com`). 대체 후보마다 왜 차선인지 한 줄 근거를 붙인다.

3. `## 사용자 직접 수행 항목` — D-03 에 따라 Claude 가 대행할 수 없는 항목만 모은 체크박스 목록(`- [ ]` 형식). 최소 다음 4개 항목을 포함한다:
   - `- [ ] 가비아 또는 후이즈에서 jodalmate.co.kr 등록 가능 여부 검색`
   - `- [ ] .co.kr 등록 자격요건 확인 (사업자등록번호 필수 여부 — RESEARCH.md Open Question 3, 미확인 상태)`
   - `- [ ] 등록 불가 시 위 대체 후보 중 선택`
   - `- [ ] 결제 완료 후 확정 도메인을 이 문서 하단 "확정 결과"에 기록`
   각 항목 아래에 "왜 Claude 가 못 하는가"를 괄호로 짧게 붙인다(결제수단 필요 / 로그인 필요).

4. `## 확정 결과 (사용자 기입란)` — 사용자가 나중에 채우는 표. 컬럼: 항목 / 값 / 확정일. 행: 서비스명(값 `조달메이트`, 확정일 기입), 영문명(값 `Jodalmate`), 도메인(값 비워둠 — `미확정` 으로 채움), 등록대행업체(비워둠), 만료일(비워둠).

5. `## 확정 후 파급 지점` — 도메인이 확정되면 갱신해야 할 하위 Phase 산출물을 목록으로 남긴다: Phase 2 배포 환경의 `NEXT_PUBLIC_SITE_URL` 류 환경변수, PWA manifest 의 `start_url`/`scope`, 이메일 알림(MATCH-02) 발신 도메인 및 SPF/DKIM 설정, Phase 3 스토어 등록 시 개인정보처리방침·이용약관 URL. 각 항목에 해당 Phase 번호를 표기한다.

문서 최상단에 `> 이 문서는 설계·운영 참고 문서이며 실행 코드가 아니다.` 한 줄을 둔다. **이 태스크는 도메인을 구매하지 않으며 구매를 시도하는 어떤 명령·스크립트도 만들지 않는다** — D-03 에 따라 구매는 전적으로 사용자 체크리스트 항목이다.
  </action>
  <reversibility rating="reversible">문서 산출물. 서비스명(D-01)은 이미 확정된 기존 결정을 기록하는 것이며, 도메인은 이 태스크에서 구매하지 않으므로 되돌릴 비용이 없다. (사용자의 실제 도메인 결제는 one-way 성격이지만 Claude 태스크가 아니다.)</reversibility>
  <verify>
    <automated>test -f "docs/design/도메인-서비스명-체크리스트.md" && for sec in "서비스명 (확정)" "도메인 방침" "사용자 직접 수행 항목" "확정 결과 (사용자 기입란)" "확정 후 파급 지점"; do grep -q "## $sec" "docs/design/도메인-서비스명-체크리스트.md" || { echo "FAIL: section $sec missing"; exit 1; }; done && grep -q "조달메이트" "docs/design/도메인-서비스명-체크리스트.md" && grep -q "jodalmate.co.kr" "docs/design/도메인-서비스명-체크리스트.md" && grep -q "D-01" "docs/design/도메인-서비스명-체크리스트.md" && grep -q "D-02" "docs/design/도메인-서비스명-체크리스트.md" && grep -q "D-03" "docs/design/도메인-서비스명-체크리스트.md" && test $(grep -c '^- \[ \]' "docs/design/도메인-서비스명-체크리스트.md") -ge 4 && echo PASS</automated>
    <human-check>가비아 또는 후이즈에서 jodalmate.co.kr 등록 가능 여부와 .co.kr 자격요건을 확인하고, 직접 결제해 도메인을 확보한 뒤 "확정 결과" 표에 기록한다. (end-of-phase 검증 시점에 확인)</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/design/도메인-서비스명-체크리스트.md` 파일이 존재한다
    - `## 서비스명 (확정)`, `## 도메인 방침`, `## 사용자 직접 수행 항목`, `## 확정 결과 (사용자 기입란)`, `## 확정 후 파급 지점` 5개 h2 섹션이 모두 존재한다
    - 문자열 `조달메이트` 와 `jodalmate.co.kr` 이 문서에 등장한다
    - 결정 ID `D-01`, `D-02`, `D-03` 이 각각 근거로 인용되어 있다
    - `- [ ]` 형식의 미완료 체크박스가 4개 이상 존재한다 (사용자 직접 수행 항목)
    - 대체 도메인 후보가 3개 이상 제시되어 있다
    - 저장소 어디에도 도메인을 구매·등록하는 스크립트나 CLI 명령 파일이 생성되지 않았다
  </acceptance_criteria>
  <done>서비스명 조달메이트가 확정 기록으로 남았고, 도메인은 후보·자격요건 확인 절차·확정 후 파급 지점까지 정리되어 사용자가 결제만 하면 되는 상태다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 저장소 → git 원격 | 이 Phase 산출물은 git 에 커밋된다(`commit_docs: true`). 문서에 기입된 값은 전부 원격 저장소로 나간다. |
| 사용자 → data.go.kr | 사용자가 외부 정부 포털에 로그인해 활용신청을 제출하고 인증키를 발급받는다. 인증키는 이후 서버 환경에만 존재해야 한다. |
| 사용자 → 도메인 등록대행업체 | 결제수단·개인정보가 외부 상용 서비스로 전달된다. Claude 는 이 경계를 넘지 않는다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `docs/design/api-신청-체크리스트.md` (git 커밋 대상) | high | mitigate | 서비스키 실제 값 기재 금지를 문서 규칙으로 명문화하고 자리표시자만 사용. Task 1 acceptance_criteria 에 "40자 이상 연속 토큰 없음" 검사를 포함. 키는 `.env` 의 `NARAJANGTEO_SERVICE_KEY` 로만 보관하고 `.gitignore` 대상임을 명시. |
| T-01-02 | Information Disclosure | 나라장터 인증키의 클라이언트 번들 유출 (Phase 2 파급) | high | mitigate | 보관 규칙 섹션에 "서버 사이드 전용, Next.js 클라이언트 번들로 내보내지 않음"을 Phase 2 구현 제약으로 선기재해 구현 단계 누락을 방지. |
| T-01-03 | Repudiation | API 승인 상태 추적 부재 | low | mitigate | 신청 상태 추적표(신청일·승인방식·승인일·상태)를 문서에 내장해 누가 언제 무엇을 신청했는지 기록으로 남긴다. |
| T-01-04 | Spoofing | 유사 도메인 선점(타이포스쿼팅)으로 인한 사칭 | low | accept | MVP 단계에서 방어 도메인 다중 등록은 비용 대비 실익이 낮다. 1인 개발 제약상 수용하고, 유료화(v2 BILL) 시점에 재검토한다. |
| T-01-05 | Tampering | 활용목적 문구가 법적 제약(낙찰 보장·입찰 대행 금지)을 위반해 서비스 포지셔닝이 왜곡됨 | medium | mitigate | 활용목적 예시 문구를 "정보 제공·보조 도구" 포지셔닝으로 작성하고, acceptance_criteria 에서 금지 표현 부재를 확인한다 (PROJECT.md Legal 제약). |
| T-01-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Phase 에는 패키지 매니저 설치 태스크가 전혀 없다 (RESEARCH.md §Package Legitimacy Audit: "해당 없음"). Phase 2 에서 실제 프레임워크·ORM 설치 시 Package Legitimacy Gate 를 반드시 적용한다. |
</threat_model>

<verification>
1. 두 산출물 파일이 `docs/design/` 아래에 존재한다.
2. 각 태스크의 `<automated>` 게이트가 PASS 를 출력한다.
3. `git diff --cached` 또는 커밋된 diff 에 실제 인증키·비밀번호가 포함되지 않았다.
4. 새로 생성된 파일 중 애플리케이션 소스코드(`.ts`, `.tsx`, `.py`, `.sql` 마이그레이션), HTML 목업이 없다 — 이 Phase 는 마크다운 문서만 산출한다.
5. (end-of-phase human verify) 사용자가 API 4건 활용신청을 제출했고 도메인 확보를 시도했다.
</verification>

<success_criteria>
- `docs/design/api-신청-체크리스트.md` 와 `docs/design/도메인-서비스명-체크리스트.md` 가 존재하고 규정된 섹션을 모두 갖췄다
- SC-1 을 달성하기 위해 사용자가 해야 할 행동이 클릭 단위로 문서화되어 있고, 승인 상태를 기록할 자리가 준비되어 있다
- SC-4 의 서비스명은 확정 기록으로, 도메인은 실행 가능한 사용자 체크리스트로 남았다
- 인증키·결제 정보 등 민감값이 git 산출물에 전혀 포함되지 않는다
</success_criteria>

<output>
Create `.planning/phases/01-prep/01-01-SUMMARY.md` when done
</output>
