---
phase: 02-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/dev.cjs
  - docker-compose.yml
  - .env.example
  - apps/api/ (nest new scaffold)
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/
  - apps/api/prisma/seed.ts
  - apps/api/tests/fixtures/announcements.sample.json
  - apps/api/src/prisma/prisma.module.ts
  - apps/api/src/prisma/prisma.service.ts
  - apps/api/test/schema.e2e-spec.ts
  - apps/web/ (create-next-app scaffold)
  - apps/web/public/manifest.json
  - apps/web/public/sw.js
autonomous: false
requirements: [CLIENT-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npm workspaces 모노레포 루트에서 npm run dev 한 번으로 API(NestJS)와 웹(Next.js)이 동시에 기동한다"
    - "9개 테이블(companies~push_subscriptions) 전체 Prisma 마이그레이션이 오류 없이 적용된다"
    - "companies 테이블에는 평문 사업자등록번호 컬럼이 존재하지 않는다 (암호문+HMAC 다이제스트 두 컬럼만 존재 — 스키마 grep으로 확인 가능)"
    - "시드 스크립트 실행 후 bid_announcements에 최소 5건의 픽스처 공고가 적재된다"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "9개 테이블 전체 스키마 (companies~push_subscriptions, password_hash 포함)"
      contains: "model companies"
    - path: "apps/api/src/prisma/prisma.service.ts"
      provides: "PrismaClient 라이프사이클 관리 (NestJS OnModuleInit/OnModuleDestroy), 02-02 이후 모든 모듈이 재사용"
    - path: "docker-compose.yml"
      provides: "로컬 PostgreSQL 16 + Redis 7 개발 환경"
    - path: "apps/api/prisma/seed.ts"
      provides: "픽스처 공고 5건 이상을 bid_announcements에 적재하는 시드 스크립트"
  key_links:
    - from: "docker-compose.yml (postgres/redis)"
      to: ".env.example (DATABASE_URL/REDIS_URL)"
      via: "docker compose up 후 DATABASE_URL로 prisma migrate deploy 실행"
      pattern: "DATABASE_URL"
    - from: "apps/api/prisma/schema.prisma"
      to: "apps/api/src/prisma/prisma.service.ts"
      via: "PrismaClient가 스키마 기반 생성 클라이언트를 그대로 사용"
      pattern: "PrismaClient"
---

## Phase Goal

**As a** 정보통신·사무용품·인쇄출판 등 목표 업종의 소규모 조달 참여 업체 담당자, **I want to** 내가 등록한 업종·지역 조건에 맞는 나라장터 신규 공고를 자동으로 매칭받아 이메일·웹푸시로 알림받고, 웹에서 검색·필터링해 확인하고 싶다, **so that** "받고 싶은 조달 공고를 놓치지 않는다"는 핵심 가치를 실제로 누릴 수 있다.

> ROADMAP.md의 Phase 2 `**Goal:**` 줄은 user-story 형식이 아니어서(`"받고 싶은 조달을 놓치지 않는다"는 핵심 가치를 매칭·알림 기능 하나로 완결시킨다`), 위 문장은 그 Goal과 `docs/design/wireframes/00-user-journey.md`의 확정된 단일 사용자 시나리오(정보통신 업종 소규모 업체 담당자의 가입→매칭→알림 여정)를 그대로 옮겨 조립한 것이며 새로 지어낸 내용이 아니다.

<objective>
Phase 2 전체가 딛고 설 지반을 만든다: npm workspaces 모노레포(apps/api NestJS + apps/web Next.js) 스캐폴딩, docker-compose 기반 로컬 PostgreSQL/Redis, `docs/design/db-schema-design.md`의 8개 테이블 + RESEARCH.md가 발견한 `push_subscriptions` 1개 = 9개 테이블 전체 Prisma 스키마와 마이그레이션, 그리고 시드 픽스처까지 실제로 동작하는 상태로 고정한다.

이 플랜은 인프라·스키마만 다룬다 — 회원가입·매칭·알림 같은 비즈니스 로직(PROF-05, MATCH-01)은 02-02가 이 위에서 구현한다. (원래 이 플랜 하나가 모노레포·스키마·인증·매칭·알림·프론트 스캐폴딩까지 27개 파일에 걸쳐 있었으나, 단일 태스크 안전 범위(15개 파일)를 크게 넘고 중간 재개 지점이 전혀 없어 gsd-plan-checker가 blocker로 지적했다 — 인프라와 비즈니스 로직 두 플랜으로 분리한다.)

체크포인트 결정값(ORM, 사업자등록번호 암호화 수단)은 SUMMARY.md에 명시적으로 기록해 02-02가 그대로 이어받는다.

Purpose: 이후 모든 플랜(02-02~02-07)이 의존할 스키마·모노레포 구조를 한 번에, 검증된 상태로 고정한다.
Output: `npm run dev`로 API+웹이 동시 기동하는 모노레포, 9테이블 Prisma 마이그레이션, 시드 픽스처 적재 확인.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/db-schema-design.md
@docs/design/업종-물품분류-매핑.md
@docs/design/wireframes/01-onboarding.md
@.planning/phases/02-mvp/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>DB ORM·마이그레이션 도구: Prisma(7.10.0 고정) vs TypeORM</decision>
  <context>
    02-RESEARCH.md는 Prisma 7.10.0(정확히 이 버전 고정 — `latest` 태그는 현재 8.0.0-rc.10 이므로 위험)을 권장하지만, Prisma 스키마 언어는 CHECK 제약을 표현하지 못해 `migrate dev --create-only` 후 SQL을 수동 추가해야 한다(Pattern 3). 이 프로젝트에 코드가 전혀 없는 지금이 ORM을 바꿀 수 있는 마지막 저비용 시점이다 — 첫 마이그레이션이 배포된 뒤 ORM을 바꾸면 전체 스키마·클라이언트 코드 재작성이 필요하다(one-way).
  </context>
  <options>
    <option id="prisma">
      <name>Prisma 7.10.0 (RESEARCH 권장)</name>
      <pros>스키마에서 TS 타입 자동 생성, 마이그레이션 diff가 읽기 쉬움, npm 레지스트리로 버전·유지보수 상태 직접 검증됨(HIGH confidence)</pros>
      <cons>CHECK 제약은 raw SQL 수동 추가 필요(워크플로우 문서화됨), `latest` 태그가 RC를 가리켜 설치 시 버전 고정 필수</cons>
    </option>
    <option id="typeorm">
      <name>TypeORM 0.3.x(legacy, 8년+ 실전검증)</name>
      <pros>CHECK 제약을 데코레이터로 직접 표현 가능</pros>
      <cons>스키마 우선 워크플로우가 아니라 엔티티 클래스 우선이라 1인 개발 인지 부하가 더 큼, RESEARCH.md는 이 옵션을 차선으로만 언급</cons>
    </option>
  </options>
  <resume-signal>Select: prisma 또는 typeorm (미응답/기본값은 prisma)</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>사업자등록번호 암호화 수단: 애플리케이션 레벨 AEAD(Node crypto AES-256-GCM) vs PostgreSQL pgcrypto</decision>
  <context>
    `docs/design/db-schema-design.md` §민감정보 저장 규칙이 최종 선택을 Phase 2로 명시적으로 위임했다("최종 선택은 Phase 2로 위임한다"). 이 결정이 저장 형식(BYTEA 컬럼 + 키 관리 위치)을 확정하므로, 데이터가 쌓인 뒤 바꾸려면 전체 재암호화 마이그레이션이 필요하다(db-schema-design.md §Phase 2 인계 사항 4, one-way). 이 결정 자체는 `apps/api/prisma/schema.prisma`의 컬럼 타입(`BYTEA`)에는 영향이 없으나, 실제 암복호화 구현(02-02의 crypto 함수)의 모양을 확정한다. RESEARCH.md는 애플리케이션 레벨 AEAD를 권장한다.
  </context>
  <options>
    <option id="app-aead">
      <name>애플리케이션 레벨 AES-256-GCM + HMAC-SHA256 (Node 내장 crypto, RESEARCH 권장)</name>
      <pros>DB가 암호문만 보고 키는 앱 서버/시크릿 매니저에만 존재 — DB 유출과 키 유출을 분리 가능, RESEARCH.md §Don't Hand-Roll이 이미 이 방식을 표준으로 지목</pros>
      <cons>키 관리(로테이션, 환경변수 보관)를 애플리케이션이 직접 책임져야 함</cons>
    </option>
    <option id="pgcrypto">
      <name>PostgreSQL pgcrypto (pgp_sym_encrypt)</name>
      <pros>DB 확장 설치만으로 사용 가능, SQL 레벨에서 암복호화</pros>
      <cons>DB 접근 권한이 있으면 복호화 키도 함께 노출될 위험 — DB 유출과 키 유출이 분리되지 않음</cons>
    </option>
  </options>
  <resume-signal>Select: app-aead 또는 pgcrypto (미응답/기본값은 app-aead)</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Tracer: 모노레포 스캐폴딩 + 9테이블 스키마 + 로컬 개발환경 기동 검증</name>
  <reversibility rating="one-way">모노레포 레이아웃(npm workspaces, apps/api+apps/web)과 9테이블 스키마는 이후 모든 플랜이 상속한다 — 위 두 checkpoint:decision이 이미 이 결정의 무게를 확인했다.</reversibility>
  <read_first>docs/design/db-schema-design.md (테이블 정의·인덱스·민감정보 저장 규칙 전체), .planning/phases/02-mvp/02-RESEARCH.md (§Standard Stack Installation, §Common Pitfalls Pitfall 1·2), docs/design/wireframes/01-onboarding.md (스텝 1)</read_first>
  <files>package.json, scripts/dev.cjs, docker-compose.yml, .env.example, apps/api/ (nest new), apps/api/prisma/schema.prisma, apps/api/prisma/migrations/, apps/api/prisma/seed.ts, apps/api/tests/fixtures/announcements.sample.json, apps/api/src/prisma/prisma.module.ts, apps/api/src/prisma/prisma.service.ts, apps/api/test/schema.e2e-spec.ts, apps/web/ (create-next-app), apps/web/public/manifest.json, apps/web/public/sw.js</files>
  <action>
    루트에 npm workspaces 모노레포를 만든다: 루트 `package.json`에 `workspaces: ["apps/api", "apps/web"]`, 루트 `scripts/dev.cjs`는 Node 내장 `child_process.spawn`만 사용해(신규 npm 의존성 추가 없음 — `concurrently` 등은 RESEARCH.md Package Legitimacy Audit 표에 없어 신규 감사 없이 도입 금지) 두 워크스페이스의 `dev` 스크립트를 동시 실행한다. 루트 `"dev"` 스크립트는 `node scripts/dev.cjs`. `docker-compose.yml`에 `postgres:16`, `redis:7` 서비스를 정의한다(포트 매핑, 볼륨). `.env.example`에 `DATABASE_URL`, `JWT_SECRET`, `BUSINESS_REG_NO_ENCRYPTION_KEY`, `BUSINESS_REG_NO_HMAC_PEPPER`(모두 `<REDACTED>` 자리표시자, db-schema-design.md §키·pepper 관리 관례 준수, 실제 값은 로컬 `.env`에서만 관리하고 이 플랜은 자리표시자만 커밋한다)를 기록한다.

    `apps/api`에 `nest new`로 NestJS 스캐폴드를 생성하고 `prisma`, `@prisma/client`를 정확히 `7.10.0`으로 고정 설치한다(위 첫 checkpoint 확정값이 prisma일 때; typeorm 확정 시 동등한 골격을 TypeORM 엔티티로 구성). `@nestjs/config`, `class-validator`, `class-transformer`, `@nestjs/passport`, `passport`, `passport-jwt`를 설치한다(02-02가 이 패키지들로 실제 인증을 구현한다) — 이 중 `@nestjs/passport`/`passport`/`passport-jwt`는 RESEARCH.md Standard Stack(Supporting) 표에 이미 인증 스택으로 명시되었고 Passport.js 자체가 10년 이상·주간 수백만 다운로드의 Node.js 표준 인증 라이브러리라 Package Legitimacy Audit 표에 없어도 추가 checkpoint 없이 승인한다(사유를 이 플랜의 SUMMARY에 기록한다). `bcrypt` 등 신규 해시 라이브러리는 이 플랜에서도, 이후 02-02에서도 설치하지 않는다 — 비밀번호 해시는 Node 내장 `crypto.scrypt`로 구현할 예정이며(02-02 범위), 신규 패키지 감사를 늘리지 않는다.

    `apps/api/prisma/schema.prisma`에 db-schema-design.md의 8개 테이블(`companies`, `company_classification_codes`, `company_performances`, `company_certifications`, `bid_announcements`, `matches`, `notification_logs`, `notification_settings`) + RESEARCH.md Pitfall 1이 찾은 `push_subscriptions` 1개 = 9개 테이블을 정의한다. `companies`에는 원본 설계에 없던 `password_hash String`(로그인용, ASVS V2 Authentication 충족을 위한 이 플랜의 재량 추가 — 와이어프레임 텍스트에 명시되지 않았으나 RESEARCH.md §Security Domain V2가 passport-jwt+비밀번호 인증을 요구함, 실제 해시 생성 로직은 02-02 범위)을 추가한다. 배열 컬럼(`region_codes`)은 Prisma의 Postgres 네이티브 배열(`String[]`)로, 금액은 `Decimal @db.Decimal(15,0)`, 시각은 `DateTime @db.Timestamptz`로 매핑한다. 첫 마이그레이션 생성 후 `migrate dev --create-only`로 빈 마이그레이션을 추가해 `company_classification_codes`·`bid_announcements`의 `classification_code` CHECK 제약(길이 2/4/6/8, 숫자 패턴) SQL을 손으로 추가한다(Pattern 3). `idx_bid_announcements_classification_code_pattern`(varchar_pattern_ops), `idx_bid_announcements_bid_close_at`, `idx_matches_company_id_matched_at` 인덱스도 이 마이그레이션에 포함한다.

    `apps/api/src/prisma/prisma.module.ts`(전역 `@Global()` 모듈)와 `prisma.service.ts`(`OnModuleInit`에서 `$connect()`, `OnModuleDestroy`에서 `$disconnect()`)를 구현해 02-02 이후 모든 모듈이 동일한 `PrismaService`를 주입받아 재사용하게 한다.

    `apps/api/tests/fixtures/announcements.sample.json`에 최소 5건의 시드 공고를 만든다: classification_code `43211501`(정보기술) 2건(하나는 region_codes에 서울 포함, 하나는 전국), `44121511`(사무용품) 1건, `551015XX`(인쇄출판) 1건, classification_code가 `null`인 1건(파싱 실패 시뮬레이션, db-schema-design.md §스파인이 강제하는 설계 제약 (c) 검증용). `prisma/seed.ts`가 이 픽스처를 `bid_announcements`에 적재하고 `npm run db:seed`로 실행 가능해야 한다.

    `apps/web`에 `create-next-app`(TypeScript, App Router)으로 스캐폴드를 생성하고 `public/manifest.json` + `public/sw.js`(최소 설치 이벤트만 처리하는 빈 서비스워커, 02-06이 fetch 캐싱 핸들러를, 02-07이 push 이벤트 핸들러를 추가) + 루트 레이아웃에서 SW 등록 스크립트를 추가해 최소 PWA 요건(CLIENT-01)을 만족시킨다. 회원가입·로그인 등 실제 화면은 02-02(가입)·02-03(로그인)이 구현한다 — 이 플랜은 빈 스캐폴드까지만 만든다.

    `apps/api/test/schema.e2e-spec.ts`를 작성해 (1) `prisma migrate deploy` 적용 후 9개 테이블이 모두 존재하는지 `information_schema.tables` 조회로 확인하고, (2) `npm run db:seed` 실행 후 `prisma.bidAnnouncement.count()`가 5 이상인지 확인하며, (3) `companies` 테이블 컬럼 목록에 평문 사업자등록번호 컬럼(`business_reg_no`)이 없고 `business_reg_no_encrypted`/`business_reg_no_digest`만 존재하는지 확인한다.
  </action>
  <verify>
    <automated>npm run test:e2e --workspace=apps/api -- schema.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/test/schema.e2e-spec.ts`가 9개 테이블 존재, 시드 5건 이상, 평문 사업자등록번호 컬럼 부재를 모두 검증하고 통과한다
    - `companies` 테이블에 평문 사업자등록번호 컬럼이 존재하지 않는다(스키마 grep으로도 확인 가능)
    - 루트에서 `node scripts/dev.cjs` 실행 시 API(NestJS)와 웹(Next.js) 두 프로세스가 모두 기동 신호를 출력한다
    - `npx prisma migrate deploy`가 오류 없이 적용된다
  </acceptance_criteria>
  <done>모노레포·9테이블 스키마·시드·로컬 개발환경 기동이 실제로 검증되었다. 02-02가 이 위에서 인증·매칭·알림 로직을 구현할 준비가 끝났다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 빌드 환경 | 신규 패키지 설치(공급망 신뢰 경계) |
| Docker 로컬 개발 환경 → 호스트 | PostgreSQL/Redis 컨테이너가 로컬 포트에 바인딩됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm install(prisma/passport 계열 등) | high | mitigate | RESEARCH.md Package Legitimacy Audit가 SUS 오탐 9건을 `time.created` 재검증으로 이미 승인함 — 추가 체크포인트 불필요. `@nestjs/passport`/`passport`/`passport-jwt`는 감사표에 없으나 10년+ 검증된 Node.js 표준 인증 스택으로 이 플랜에서 승인 처리(사유 SUMMARY에 기록). Prisma는 반드시 `7.10.0` 고정 설치(latest=RC 위험) |
| T-02-20 | Information Disclosure | docker-compose 기본 계정/포트가 로컬에 노출 | low | accept | 로컬 개발 전용 구성이며 `.env.example`에는 자리표시자만 커밋, 실제 값은 gitignore된 `.env`에만 존재 — 운영 배포 시 별도 시크릿 관리로 교체(인프라 Phase 범위) |
</threat_model>

<verification>
- `npm run test:e2e --workspace=apps/api -- schema.e2e-spec.ts` 통과
- Prisma 마이그레이션이 오류 없이 적용된다: `npx prisma migrate deploy`
- `node scripts/dev.cjs`로 두 워크스페이스가 동시 기동한다
- 회원가입·로그인·매칭 등 비즈니스 로직 테스트는 02-02·02-03 범위
</verification>

<success_criteria>
9테이블 스키마·모노레포·로컬 개발 환경이 실코드로 검증되며, 이후 6개 플랜(02-02~02-07)이 상속할 기반이 고정된다.
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-01-SUMMARY.md` when done
</output>
