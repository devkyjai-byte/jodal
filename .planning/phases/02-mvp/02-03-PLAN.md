---
phase: 02-mvp
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/dto/login.dto.ts
  - apps/api/src/auth/verification/nts-verification.port.ts
  - apps/api/src/auth/verification/nts-verification.adapter.ts
  - apps/web/app/login/page.tsx
  - apps/web/app/signup/page.tsx
autonomous: true
requirements: [PROF-05, CLIENT-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "가입한 업체는 이메일·비밀번호로 로그인해 JWT를 재발급받을 수 있다"
    - "잘못된 비밀번호를 입력해도 계정 존재 여부가 노출되지 않는다 (동일한 401 문구)"
    - "국세청 진위확인 API가 지연·실패해도 가입 응답(201) 자체는 영향받지 않는다"
    - "재방문 시 사업자등록번호 입력 필드는 앞 3자리만 노출하고 마스킹되어 표시된다"
  artifacts:
    - path: "apps/api/src/auth/auth.controller.ts"
      provides: "POST /auth/login"
    - path: "apps/api/src/auth/verification/nts-verification.adapter.ts"
      provides: "국세청 사업자등록정보 진위확인 API 비동기 연동"
    - path: "apps/web/app/login/page.tsx"
      provides: "로그인 화면"
  key_links:
    - from: "apps/web/app/login/page.tsx"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "POST /auth/login { email, password }"
      pattern: "/auth/login"
    - from: "apps/api/src/auth/auth.service.ts (signup, 02-02)"
      to: "apps/api/src/auth/verification/nts-verification.adapter.ts"
      via: "가입 응답 반환 직후 non-blocking 호출, verification_status 비동기 갱신"
      pattern: "NtsVerificationPort"
---

<objective>
02-02가 만든 `AuthService.signup()`(JWT 발급까지만) 뒤에 실제 재로그인 경로와 PROF-05의 "인증"(사업자등록번호 진위확인) 절반을 완성한다. `01-onboarding.md`가 요구하는 두 엣지 케이스 — 진위확인 API 지연·실패, 사업자등록번호 마스킹 재노출 — 를 여기서 구현한다.

Purpose: PROF-05("가입·인증")를 완전히 충족시키고, 사용자가 세션 만료 후에도 서비스로 복귀할 수 있게 한다.
Output: 로그인 엔드포인트+화면, 비동기 국세청 진위확인 연동, 온보딩 스텝1의 마스킹·중복가입 UX.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design/wireframes/01-onboarding.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>로그인 엔드포인트 + 화면 end-to-end</name>
  <precondition>02-02의 AuthService.signup()과 password.crypto.ts(scrypt), jwt.strategy.ts/jwt-auth.guard.ts가 존재한다</precondition>
  <read_first>docs/design/wireframes/01-onboarding.md (§엣지 케이스 "이미 가입된 사업자등록번호로 재가입 시도"), .planning/phases/02-mvp/02-02-SUMMARY.md</read_first>
  <behavior>
    - 올바른 email+password → 200 + JWT(payload에 companyId)
    - 존재하지 않는 email → 401, 문구는 "이메일 또는 비밀번호가 올바르지 않습니다"
    - 존재하는 email + 틀린 password → 동일한 401 + 동일 문구(사용자 열거 방지, 응답 시간도 두 실패 케이스가 비슷하도록 scrypt 비교를 항상 실행 — 존재하지 않는 email이어도 더미 해시와 비교해 타이밍 차이를 줄인다)
  </behavior>
  <files>apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.service.ts, apps/api/src/auth/dto/login.dto.ts, apps/web/app/login/page.tsx</files>
  <action>
    `POST /auth/login`(`{ email, password }`)를 구현한다: `companies.contact_email`로 조회 후 `password.crypto.ts`의 scrypt 비교 함수로 검증, 성공 시 02-02와 동일한 `jwt.strategy.ts` 서명 방식으로 JWT 발급. `apps/web/app/login/page.tsx`에서 `lib/api-client.ts`(02-02)를 통해 이 엔드포인트를 호출하고 응답 JWT를 저장 후 `/feed`로 이동한다. 이메일을 1차 로그인 식별자로 채택한 이유: 와이어프레임에 로그인 화면 필드가 명시되어 있지 않아 이 플랜의 재량으로 결정 — `companies.contact_email`이 이미 NOT NULL이고, 사업자등록번호를 로그인 폼에 다시 입력시키면 마스킹 규칙(아래 두 번째 태스크)과 충돌하기 때문이다.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- auth.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 위 behavior 3가지 케이스가 모두 테스트로 통과한다
    - 로그인 화면에서 성공 시 `/feed`로 이동한다
  </acceptance_criteria>
  <done>로그인 API+화면이 동작하고 사용자 열거를 방지한다.</done>
</task>

<task type="auto">
  <name>국세청 진위확인 비동기 연동 + 온보딩 스텝1 엣지 케이스</name>
  <read_first>.planning/phases/02-mvp/02-RESEARCH.md (§Code Examples 사업자등록번호 진위확인 API 호출, §Open Questions 2, §Assumptions Log A3), docs/design/wireframes/01-onboarding.md (§엣지 케이스 전체)</read_first>
  <files>apps/api/src/auth/verification/nts-verification.port.ts, apps/api/src/auth/verification/nts-verification.adapter.ts, apps/api/src/auth/auth.service.ts, apps/web/app/signup/page.tsx</files>
  <action>
    `NtsVerificationPort`(`verify(bizNo, openDate, repName): Promise<'verified'|'failed'>`) 인터페이스와 실제 국세청 사업자등록정보 진위확인 API를 호출하는 `NtsVerificationAdapter`(Node 내장 `fetch`, `NTS_API_KEY` 환경변수, RESEARCH.md §Code Examples 패턴 — 정확한 엔드포인트·파라미터명은 Open Question 2로 미확정이므로 응답 파싱 실패를 레코드 단위 예외로 잡아 `verification_status`를 변경하지 않고 로그만 남긴다)를 구현한다. `AuthService.signup()`이 201 응답을 반환한 직후(await 하지 않음, non-blocking) 이 어댑터를 호출해 성공 시에만 `companies.verification_status`/`verified_at`을 갱신한다 — 지연·실패해도 가입 자체를 막지 않는다(01-onboarding.md §엣지 케이스).

    `apps/web/app/signup/page.tsx`에 사업자등록번호 재방문 시 마스킹 표시(`123-**-*****`, 앞 3자리만 노출 — 뒤 7자리는 서버에서 절대 평문으로 재전송하지 않고 프론트가 로컬에서 마스킹 문자열만 렌더링)와, 409(중복 가입) 응답 처리("이미 등록된 사업자등록번호입니다. 로그인하시겠어요?" + `/login` 링크)를 추가한다. 개인정보 동의 체크박스 미체크 시 인라인 오류(01-onboarding.md §엣지 케이스)도 이 화면에서 마무리한다.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- auth.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 국세청 API가 네트워크 오류를 던지도록 모킹해도 signup의 HTTP 응답(201)에는 영향이 없다(비동기 예외가 요청-응답 사이클을 막지 않음을 단위테스트로 검증)
    - 중복 사업자등록번호로 가입 시도 시 409 + "로그인하시겠어요?" 링크가 화면에 표시된다
    - 개인정보 동의 미체크 상태로 제출 시 API 호출 자체가 발생하지 않는다(프론트 검증)
  </acceptance_criteria>
  <done>진위확인이 비동기로 안전하게 동작하고, 온보딩 스텝1의 모든 엣지 케이스가 처리된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /auth/login | 미신뢰 자격증명 입력, 무차별 대입 공격 표면 |
| NestJS → 국세청 진위확인 API(외부) | 비동기, 사업자등록번호 1개 필드만 전송, 응답 원문 미저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Information Disclosure | 로그인 실패 응답으로 계정 존재 여부 열거 | medium | mitigate | 이메일 미존재/비밀번호 불일치를 동일 401+동일 문구로 응답, 미존재 시에도 더미 해시 비교로 타이밍 차이 최소화 |
| T-02-19 | Denial of Service | 로그인 엔드포인트 무차별 대입 | medium | accept | MVP 범위에서 rate limiting 보류(T-02-05와 동일 판단) — scrypt 자체의 계산 비용이 1차 완화 역할을 함 |
</threat_model>

<verification>
- `npm run test --workspace=apps/api -- auth.controller.spec.ts auth.service.spec.ts` 통과
</verification>

<success_criteria>
PROF-05("가입·인증")가 로그인+진위확인까지 포함해 완전히 충족되고, 온보딩 스텝1의 모든 엣지 케이스가 구현된다.
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-03-SUMMARY.md` when done
</output>
