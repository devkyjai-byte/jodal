---
phase: 02-mvp
plan: 06
type: execute
wave: 4
depends_on: ["02-01", "02-04", "02-05"]
files_modified:
  - apps/api/src/notifications/adapters/resend-email.adapter.ts
  - apps/api/src/notifications/notify.processor.ts
  - apps/api/src/notifications/notifications.service.ts
  - apps/api/src/notifications/push-subscriptions.controller.ts
  - apps/api/src/notifications/web-push.service.ts
  - apps/api/src/notifications/notification-settings.controller.ts
  - apps/api/src/notifications/notification-logs.controller.ts
  - apps/web/public/sw.js
  - apps/web/app/settings/notifications/page.tsx
  - apps/web/components/settings/PushSubscribeButton.tsx
  - .env.example
autonomous: true
requirements: [MATCH-02, MATCH-03, CLIENT-01]

estimate:
  tokens: 87000
  raw_tokens: 87000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "적합도 임계값 이상 신규 매칭에 대해 실제 이메일이 Resend로 발송되고 notification_logs에 정확히 1건만 기록된다 (재시도해도 중복 발송되지 않는다)"
    - "업체가 웹 푸시 구독을 허용하면 신규 매칭 시 브라우저 푸시 알림을 받는다"
    - "업체는 알림 설정 화면에서 채널·임계값·빈도·방해금지시간·마감임박리마인더를 직접 조정할 수 있고 변경이 즉시 저장된다"
    - "이메일·푸시를 모두 끄려 하면 확인 다이얼로그가 먼저 뜬다"
    - "방해금지 시간대에 발생한 알림은 버려지지 않고 시간대 종료 후 발송된다"
  artifacts:
    - path: "apps/api/src/notifications/adapters/resend-email.adapter.ts"
      provides: "EmailSenderPort의 실제 프로덕션 구현 (02-01 ConsoleEmailAdapter 대체)"
    - path: "apps/api/src/notifications/notify.processor.ts"
      provides: "BullMQ notify 큐 컨슈머 — 이메일+푸시 실제 발송"
    - path: "apps/api/src/notifications/web-push.service.ts"
      provides: "VAPID 기반 웹 푸시 발송"
    - path: "apps/web/app/settings/notifications/page.tsx"
      provides: "알림 설정 화면 (CLIENT-01)"
  key_links:
    - from: "apps/api/src/queues/queues.module.ts (notify 큐, 02-04에서 정의)"
      to: "apps/api/src/notifications/notify.processor.ts"
      via: "@Processor('notify') 컨슈머 등록"
      pattern: "@Processor\\('notify'\\)"
    - from: "apps/api/src/notifications/notify.processor.ts"
      to: "apps/api/src/notifications/adapters/resend-email.adapter.ts"
      via: "EmailSenderPort 인터페이스 (02-01에서 정의됨)"
      pattern: "EmailSenderPort"
    - from: "apps/web/app/settings/notifications/page.tsx"
      to: "apps/api/src/notifications/notification-settings.controller.ts"
      via: "GET/PATCH /notification-settings"
      pattern: "notification-settings"
---

<objective>
02-01의 `ConsoleEmailAdapter`(개발용 스텁)를 실제 Resend 발송으로 교체하고, 웹 푸시(MATCH-03)를 새로 추가하며, `04-notification-settings.md`가 서술한 알림 설정 화면 전체를 구현한다. `EmailSenderPort` 인터페이스는 02-01이 이미 정의했으므로 이 플랜은 어댑터만 교체한다 — 아키텍처 변경이 아니다.

Purpose: "필요한 만큼만 알림을 받는다"는 04-notification-settings.md의 전제를 실제 발송 인프라로 완성한다.
Output: 실제 이메일·웹푸시 발송, 알림 설정 화면, 발송 이력 조회.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design/wireframes/04-notification-settings.md
@docs/design/db-schema-design.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-01-SUMMARY.md
@.planning/phases/02-mvp/02-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>실제 이메일 발송 워커 — notify 큐 컨슈머 + Resend 어댑터 + 방해금지 시간대</name>
  <precondition>02-04의 notify 큐(queues.module.ts)가 정의되어 있고, 02-01의 EmailSenderPort/ConsoleEmailAdapter가 존재한다</precondition>
  <read_first>.planning/phases/02-mvp/02-RESEARCH.md (§Architecture Patterns Pattern 1, §Standard Stack resend 버전), docs/design/wireframes/04-notification-settings.md (§엣지 케이스 "방해금지 시간대에 발생한 알림의 처리 방침")</read_first>
  <files>apps/api/src/notifications/adapters/resend-email.adapter.ts, apps/api/src/notifications/notify.processor.ts, apps/api/src/notifications/notifications.service.ts, .env.example</files>
  <action>
    `resend@6.22.1`(RESEARCH.md 승인됨)을 설치하고 `EmailSenderPort`를 구현하는 `ResendEmailAdapter`를 만든다(`RESEND_API_KEY` 환경변수). `NotificationsModule`의 프로바이더 등록을 `NODE_ENV`/`EMAIL_ADAPTER` 환경변수로 `ConsoleEmailAdapter`(개발/테스트)와 `ResendEmailAdapter`(운영) 사이에서 선택하도록 팩토리 프로바이더로 바꾼다(02-01의 인터페이스를 그대로 재사용 — 새 인터페이스를 만들지 않는다).

    `notify.processor.ts`(`@Processor('notify')`)가 `dispatch-notifications` 잡(`{ matchIds }`, 02-04의 match.processor가 enqueue)을 받아 각 `match`에 대해 `notification_settings`를 조회한다: (1) `email_enabled`이고 아직 `notification_logs(match_id, channel='email')`가 없으면 발송, (2) 현재 시각이 `quiet_hours_start`~`quiet_hours_end` 구간이면 즉시 버리지 않고 그 구간 종료 시각으로 `notifyQueue.add(..., { delay })`를 다시 예약한다(버리지 않음 — 04-notification-settings.md §엣지 케이스), (3) `digest_frequency='daily_digest'`인 업체는 즉시 발송 대신 별도의 일일 요약 잡 대상으로 표시만 하고(이 플랜에서 일일 요약 발송 잡까지 새로 만들지는 않는다 — `immediate`가 기본값이며 04-notification-settings.md는 두 옵션 존재만 요구하고 요약 발송 스케줄러 세부사항은 규정하지 않음, 이 범위 판단은 Claude's Discretion), (4) 발송 성공/실패와 무관하게 `notification_logs`를 `UNIQUE(match_id, channel)` 기준 UPSERT(재시도해도 새 행 생성 없음, db-schema-design.md §스파인이 강제하는 설계 제약 (d)).
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- notify.processor.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 동일 `matchId`로 `dispatch-notifications` 잡을 두 번 실행해도 `notification_logs` 행이 1개만 존재한다(상태만 갱신)
    - 방해금지 시간대 안에 트리거된 알림은 그 시점에 발송되지 않고, 시간대 종료 이후로 재예약된 잡 데이터가 큐에서 확인된다(테스트에서 `delay` 값 검증)
    - `EMAIL_ADAPTER=console`이면 실제 Resend API가 호출되지 않는다(테스트 환경 안전)
  </acceptance_criteria>
  <done>실제 이메일 발송이 멱등성·방해금지 규칙을 지키며 동작한다.</done>
</task>

<task type="auto" tdd="true">
  <name>웹 푸시 구독·발송(MATCH-03)</name>
  <precondition>apps/web/public/sw.js에 02-05가 추가한 GET /feed 캐싱 fetch 핸들러가 이미 존재한다 (동일 파일에 push 핸들러를 추가하는 것이므로 02-05 이후에 실행)</precondition>
  <read_first>.planning/phases/02-mvp/02-RESEARCH.md (§Architecture Patterns Pattern 2 웹 푸시 VAPID, §Common Pitfalls Pitfall 1 push_subscriptions)</read_first>
  <files>apps/api/src/notifications/push-subscriptions.controller.ts, apps/api/src/notifications/web-push.service.ts, apps/api/src/notifications/notify.processor.ts, apps/web/public/sw.js, apps/web/components/settings/PushSubscribeButton.tsx, .env.example</files>
  <behavior>
    - `web-push.service.ts`: VAPID 키 쌍이 `.env`에 없으면 1회 생성해 안내(재생성 시 기존 구독 전부 무효화된다는 경고 로그 포함) — 있으면 재사용
    - `POST /push-subscriptions`(JWT): endpoint/p256dh/auth 저장, 동일 endpoint 재구독 시 UPSERT(UNIQUE 제약)
    - `DELETE /push-subscriptions/:id`: 소유권 검증 후 삭제
    - `notify.processor.ts`가 `push_enabled`인 업체의 모든 `push_subscriptions`에 `web-push.sendNotification()`을 호출하고, `410 Gone`(구독 만료) 응답을 받으면 해당 구독 행을 삭제
  </behavior>
  <action>
    `web-push@3.6.7`(RESEARCH.md 승인됨)을 설치한다. `push-subscriptions.controller.ts`를 위 behavior대로 구현한다. `notify.processor.ts`에 push 채널 처리를 추가해 이메일과 동일하게 `notification_logs(channel='push')`를 UPSERT한다. `apps/web/public/sw.js`(02-05가 만든 fetch 캐싱 핸들러에 추가)에 `push` 이벤트 리스너를 추가해 수신한 payload(JSON: title, announcementId)로 `showNotification()`을 호출하고, 알림 클릭 시 `/announcements/{announcementId}`로 이동하는 `notificationclick` 핸들러를 추가한다. `PushSubscribeButton.tsx`: 클릭 시 `Notification.requestPermission()` → 허용되면 `serviceWorker.pushManager.subscribe({ applicationServerKey: VAPID_PUBLIC_KEY })` → `POST /push-subscriptions`. 거부 시 토글을 되돌리고 안내 문구를 표시한다(04-notification-settings.md §엣지 케이스 "브라우저 푸시 권한 거부·차단").
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- push-subscriptions.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 동일 endpoint로 재구독해도 `push_subscriptions` 행이 늘어나지 않는다(UPSERT)
    - `web-push.sendNotification()`이 410을 던지면 해당 구독 행이 삭제된다(테스트에서 모킹)
    - 다른 업체 소유의 구독 id로 DELETE 시도 시 403
  </acceptance_criteria>
  <done>웹 푸시 구독·발송·만료 정리가 동작한다.</done>
</task>

<task type="auto">
  <name>알림 설정 화면 — 채널·임계값·빈도·방해금지·이력</name>
  <read_first>docs/design/wireframes/04-notification-settings.md (전체)</read_first>
  <files>apps/api/src/notifications/notification-settings.controller.ts, apps/api/src/notifications/notification-logs.controller.ts, apps/web/app/settings/notifications/page.tsx</files>
  <action>
    `GET/PATCH /notification-settings`(JWT, 항목별 부분 갱신 허용 — 토글 하나 바뀔 때마다 즉시 저장되는 UX를 지원하기 위해 PATCH는 각 필드를 선택적으로 받는다)와 `GET /notification-settings/preview?threshold=N`(최근 7일 매칭 중 점수 ≥ threshold인 건수를 반환 — 슬라이더 "예상 알림량" 안내용)를 구현한다. `GET /notification-logs`(최근 발송 이력, 채널·발송시각·상태)를 구현한다. 이메일 채널에 대해 최근 연속 `status='failed'` 행이 3건 이상이면 `bounceWarning: true`를 `GET /notification-settings` 응답에 포함한다(04-notification-settings.md §엣지 케이스 "이메일 반송 상태인 주소" — 전용 반송 웹훅 없이 발송 실패 이력으로 근사, Claude's Discretion).

    `apps/web/app/settings/notifications/page.tsx`: 채널(이메일/푸시 토글, 변경 즉시 PATCH+토스트), 적합도 임계값 슬라이더(등급 5단계 눈금, 조정 중 `/notification-settings/preview` 호출로 "최근 7일 기준 약 N건" 실시간 안내), 발송 빈도(즉시/일간 요약 라디오), 방해금지 시작·종료 시각(자정 넘김 자동 해석), 마감임박 리마인더(on/off+며칠전), 최근 발송 이력(펼침형 표), 카카오 알림톡 "추후 지원 예정" 한 줄 고지를 구현한다. 이메일·푸시 모두 off로 만들려는 시도는 확인 다이얼로그를 먼저 띄우고, 확인 후에도 상단에 "모든 알림이 꺼져 있습니다" 경고 배너를 지속 노출한다. 저장 실패 시 낙관적 업데이트를 롤백하고 토스트를 표시한다. `?from=detail&hint={prefix}` 쿼리로 진입하면 임계값 슬라이더를 강조 표시한다(00-user-journey.md §화면 간 데이터 인계 계약).
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- notification-settings.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 이메일 토글을 끈 뒤 GET으로 재조회하면 `email_enabled: false`가 즉시 반영된다
    - 이메일·푸시를 모두 끄려는 PATCH 요청은 프론트에서 확인 다이얼로그 없이는 전송되지 않는다(프론트 로직으로 검증, API 자체는 막지 않음 — 04-notification-settings.md가 이를 UX 확인 절차로 규정)
    - `bounceWarning`이 연속 3회 실패 시 true로 반환된다(단위테스트로 notification_logs 픽스처 검증)
    - 방해금지 종료 시각이 시작 시각보다 이르면 자정을 넘기는 구간으로 저장된다
  </acceptance_criteria>
  <done>알림 설정 화면이 모든 항목·엣지케이스를 포함해 완성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NestJS 워커 → Resend/web-push(외부 서비스) | 이메일 본문에 공고명·업체 담당자 이메일 포함, API 키는 서버에만 존재 |
| Browser → PATCH /notification-settings, POST /push-subscriptions | 인증된 업체 본인 설정만 변경 가능해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Spoofing | 웹 푸시 구독 엔드포인트 탈취·재사용 | medium | mitigate | `push_subscriptions.endpoint` UNIQUE + company_id 소유권 검증, 발송은 내부 큐에서만 트리거(외부 발송 API 비공개) — RESEARCH.md §Known Threat Patterns |
| T-02-16 | Information Disclosure | Resend/web-push API 키·VAPID 개인키 유출 | high | mitigate | `.env`/`@nestjs/config`에만 보관, 로그에 이메일 본문 전체 미기록 |
| T-02-17 | Tampering | notification_settings를 타 업체가 변경 | high | mitigate | PATCH 시 JWT의 company_id로만 자신의 행을 갱신(WHERE company_id) |
| T-02-SC | Tampering | npm install(resend, web-push) | high | accept | RESEARCH.md Package Legitimacy Audit에서 이미 승인됨 — 추가 체크포인트 불필요 |
</threat_model>

<verification>
- `npm run test --workspace=apps/api` 전체 통과 (notify.processor.spec.ts, push-subscriptions.controller.spec.ts, notification-settings.controller.spec.ts 포함)
- 알림 설정 화면의 8개 항목이 모두 실제 컬럼과 1:1로 연결됨을 수동 확인 후 SUMMARY 기록
</verification>

<success_criteria>
MATCH-02(이메일)·MATCH-03(웹푸시)가 실제 발송으로 완성되고, 알림 설정 화면이 04-notification-settings.md의 모든 항목·엣지 케이스를 구현한다.
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-06-SUMMARY.md` when done
</output>
