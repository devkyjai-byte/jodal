---
phase: 02-mvp
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/api/src/auth/auth.module.ts
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/jwt.strategy.ts
  - apps/api/src/auth/jwt-auth.guard.ts
  - apps/api/src/auth/crypto/business-reg-no.crypto.ts
  - apps/api/src/auth/crypto/password.crypto.ts
  - apps/api/src/companies/classification-codes.controller.ts
  - apps/api/src/companies/companies.service.ts
  - apps/api/src/matching/matching.service.ts
  - apps/api/src/notifications/ports/email-sender.port.ts
  - apps/api/src/notifications/adapters/console-email.adapter.ts
  - apps/api/src/notifications/notifications.service.ts
  - apps/api/test/tracer.e2e-spec.ts
  - apps/web/app/signup/page.tsx
  - apps/web/lib/api-client.ts
autonomous: true
requirements: [PROF-05, MATCH-01, CLIENT-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "신규 업체가 사업자등록번호·업체명·이메일·비밀번호로 가입하면 계정이 즉시 생성되고 JWT가 발급된다 (실제 로그인 엔드포인트는 02-03에서 완성)"
    - "이미 등록된 사업자등록번호(다이제스트 기준)로 재가입을 시도하면 409로 거부된다"
    - "가입한 업체가 업종 분류코드를 1개 이상 등록하면, 그 prefix로 시작하는 시드 공고와 자동으로 매칭되어 matches 행이 생성된다"
    - "적합도 임계값(기본 60점) 이상 매칭에 대해 이메일 발송 기록이 notification_logs에 sent 상태로 정확히 1건만 남는다"
    - "JWT 없이 보호된 프로필 엔드포인트를 호출하면 401이 반환된다"
  artifacts:
    - path: "apps/api/src/auth/auth.service.ts"
      provides: "회원가입, 사업자등록번호 암호화+다이제스트, 비밀번호 해시, JWT 발급 (로그인 엔드포인트는 02-03)"
    - path: "apps/api/src/matching/matching.service.ts"
      provides: "scoreMatch/toQualitativeTier/scoreAndUpsert"
      exports: ["MatchingService"]
    - path: "apps/api/src/notifications/notifications.service.ts"
      provides: "임계값 판정 + 이메일 발송 + notification_logs upsert"
    - path: "apps/api/test/tracer.e2e-spec.ts"
      provides: "가입→업종등록→매칭→알림기록 end-to-end 검증"
  key_links:
    - from: "apps/api/src/auth/auth.service.ts (signup)"
      to: "apps/api/prisma/schema.prisma (companies, notification_settings — 02-01에서 마이그레이션됨)"
      via: "prisma.company.create + 중첩 notification_settings.create"
      pattern: "prisma\\.compan(y|ies)\\.create"
    - from: "apps/api/src/companies/classification-codes.controller.ts"
      to: "apps/api/src/matching/matching.service.ts"
      via: "등록 직후 scoreAndUpsert(companyId) 동기 호출"
      pattern: "scoreAndUpsert"
    - from: "apps/api/src/matching/matching.service.ts"
      to: "apps/api/src/notifications/notifications.service.ts"
      via: "매칭 upsert 직후 sendMatchNotifications(companyId) 동기 호출"
      pattern: "sendMatchNotifications"
---

<objective>
02-01이 고정한 모노레포·9테이블 스키마 위에 Phase 2의 핵심 비즈니스 로직을 실제로 동작하는 코드로 증명한다: "회원가입(PROF-05 일부) → 업종코드 등록 → 시드 공고와 자동 매칭(MATCH-01) → 이메일 알림 기록"까지 이어지는 최소 1개 경로를 end-to-end로 완성한다.

이 플랜이 만드는 `matching.service.ts`와 `notifications.service.ts`는 스텁이 아니다 — 02-05(배치 수집)가 이 서비스를 BullMQ 워커 뒤에 그대로 연결하고, 02-07(알림 발송)이 콘솔 어댑터를 Resend/web-push 어댑터로 교체할 뿐, 아키텍처를 바꾸지 않는다. 로그인(`POST /auth/login`)은 이 플랜 범위가 아니다 — 02-03이 완성한다.

Purpose: PROF-05의 가입 절반과 MATCH-01의 최초 스코어링 경로를 검증된 상태로 고정해, 이후 모든 플랜(02-03~02-07)이 이 위에서 확장하게 한다.
Output: 회원가입 API+화면, 사업자등록번호 암호화, JWT 발급/가드, 매칭 스코어링 엔진, 알림 발송 스켈레톤, 가입~매칭~알림기록 e2e 테스트 통과.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/db-schema-design.md
@docs/design/업종-물품분류-매핑.md
@docs/design/wireframes/01-onboarding.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Tracer: 회원가입 → 업종등록 → 매칭 → 알림기록 end-to-end (백엔드)</name>
  <precondition>02-01의 9테이블 Prisma 마이그레이션과 PrismaService, npm workspaces 모노레포가 존재한다 (02-01 schema.e2e-spec.ts green)</precondition>
  <read_first>docs/design/db-schema-design.md (테이블 정의·민감정보 저장 규칙 전체), .planning/phases/02-mvp/02-RESEARCH.md (§Code Examples MATCH-01 스코어링 함수, §Common Pitfalls Pitfall 1·2), .planning/phases/02-mvp/02-01-SUMMARY.md (체크포인트 확정값: ORM·암호화 수단), docs/design/wireframes/01-onboarding.md (스텝 1)</read_first>
  <files>apps/api/src/auth/auth.module.ts, apps/api/src/auth/auth.service.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/jwt.strategy.ts, apps/api/src/auth/jwt-auth.guard.ts, apps/api/src/auth/crypto/business-reg-no.crypto.ts, apps/api/src/auth/crypto/password.crypto.ts, apps/api/src/companies/classification-codes.controller.ts, apps/api/src/companies/companies.service.ts, apps/api/src/matching/matching.service.ts, apps/api/src/notifications/ports/email-sender.port.ts, apps/api/src/notifications/adapters/console-email.adapter.ts, apps/api/src/notifications/notifications.service.ts, apps/api/test/tracer.e2e-spec.ts</files>
  <action>
    `AuthService.signup()`: 사업자등록번호 형식 검증(10자리 숫자 + 체크섬), 02-01 checkpoint 확정값에 따라 `business-reg-no.crypto.ts`에 AES-256-GCM 암호화(`business_reg_no_encrypted`)와 `HMAC-SHA256(bizNo, pepper)` 다이제스트(`business_reg_no_digest`) 함수를 구현(단순 SHA-256 절대 금지 — db-schema-design.md §민감정보 저장 규칙), `password.crypto.ts`에 `crypto.scrypt` 해시 함수를 구현한다(`bcrypt` 등 신규 패키지 설치 없음 — Node 내장 `crypto`만 사용, 예: `scrypt$N$r$p$saltHex$hashHex` 형식 단일 문자열). 다이제스트 UNIQUE 위반 시 409 반환. 가입 성공 시 `companies` 행 + 기본값 `notification_settings` 행(`email_enabled=true`)을 하나의 트랜잭션으로 생성하고 JWT를 반환한다. `jwt.strategy.ts`(`passport-jwt` `Strategy`, `JWT_SECRET` 환경변수로 서명 검증, payload에 `companyId`만 담음)와 `jwt-auth.guard.ts`(`AuthGuard('jwt')` 래퍼)를 구현해 아래 `classification-codes` 엔드포인트와 이후 모든 플랜의 인증 필요 엔드포인트가 공통으로 재사용한다. `main.ts`에서 `app.enableCors()`로 개발 중 Next.js 프론트(다른 포트)의 크로스오리진 요청을 허용한다.

    `POST /companies/me/classification-codes`(JWT 가드 적용): 자릿수(2/4/6/8)·숫자 패턴 검증 후 `company_classification_codes` 행 생성, 생성 직후 `MatchingService.scoreAndUpsert(companyId)`를 동기 호출한다. `matching.service.ts`는 RESEARCH.md §Code Examples의 `scoreMatch`(prefix 최대60·지역 최대25·실적/인증 최대15) + `toQualitativeTier`(5단계) 공식을 그대로 구현하고, 이 업체의 등록 prefix로 시작하는 `bid_announcements`를 `classification_code LIKE '{prefix}%'` 리터럴 OR절로 조회해(db-schema-design.md §스파인이 강제하는 설계 제약 (b) 권장안) `matches`를 UPSERT한다. 점수(원점수)는 API 응답에 절대 그대로 노출하지 않는다 — 등급 문자열로만 변환해 반환한다(Legal 제약).

    `matches` upsert 직후 `NotificationsService.sendMatchNotifications(companyId)`를 동기 호출한다: `notification_settings`를 조회해 `email_enabled && score >= min_score_threshold`인 신규 매칭만 골라, `EmailSenderPort` 인터페이스(이 플랜은 `ConsoleEmailAdapter`만 구현 — 콘솔에 로그하고 즉시 resolve, 02-07이 `ResendEmailAdapter`로 교체)로 발송한 뒤 `notification_logs`를 `UNIQUE(match_id, channel)` 기준으로 UPSERT(재시도 시 새 행 대신 상태 갱신, db-schema-design.md §스파인이 강제하는 설계 제약 (d)).
  </action>
  <verify>
    <automated>npm run test:e2e --workspace=apps/api -- tracer.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/test/tracer.e2e-spec.ts`가 다음을 순서대로 검증한다: (1) `POST /auth/signup` 201 + JWT 반환, (2) 동일 사업자등록번호로 재요청 시 409, (3) `POST /companies/me/classification-codes {code:"43"}` 201, (4) DB에 `matches` 행이 생성되고 `score > 0`, (5) `notification_logs`에 `channel='email', status='sent'` 행이 정확히 1건 존재
    - JWT 없이 `POST /companies/me/classification-codes` 호출 시 401(jwt-auth.guard.ts가 실제로 엔드포인트를 보호함)
    - `companies` 테이블에 평문 사업자등록번호 컬럼이 여전히 존재하지 않는다(02-01 스키마 그대로 사용)
  </acceptance_criteria>
  <done>가입→업종등록→매칭→알림기록 전체 경로가 실제 코드로 동작하고 e2e 테스트가 통과한다.</done>
</task>

<task type="auto">
  <name>회원가입 화면 연결 (Next.js)</name>
  <read_first>docs/design/wireframes/01-onboarding.md (스텝 1)</read_first>
  <files>apps/web/app/signup/page.tsx, apps/web/lib/api-client.ts</files>
  <action>
    `apps/web/app/signup/page.tsx`에 사업자등록번호·업체명·이메일·비밀번호·개인정보 수집·이용 동의 체크박스 폼을 만들어 `lib/api-client.ts`(fetch 래퍼, `NEXT_PUBLIC_API_URL` 환경변수 사용)를 통해 `POST /auth/signup`을 호출하고 응답 JWT를 저장한다. 동의 미체크·형식 오류 시 인라인 오류를 표시한다(01-onboarding.md §엣지 케이스). 사업자등록번호 마스킹 재노출과 409(중복 가입) 안내는 02-03이 이어서 구현한다 — 이 태스크는 최초 제출 흐름만 완성한다.
  </action>
  <verify>
    <automated>npm run test:e2e --workspace=apps/api -- tracer.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 회원가입 폼 제출 시 `POST /auth/signup`이 호출되고 성공 시 JWT가 로컬에 저장된다(수동 확인, SUMMARY에 기록)
    - 개인정보 동의 미체크 상태로 제출 시 API 호출 자체가 발생하지 않고 인라인 오류가 표시된다
  </acceptance_criteria>
  <done>회원가입 화면이 위 태스크의 백엔드 엔드포인트에 실제로 연결된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser(Next.js 회원가입) → NestJS API | 사업자등록번호·비밀번호 등 미신뢰 사용자 입력이 넘어옴 |
| NestJS API → PostgreSQL | 암호화된 사업자등록번호·비밀번호 해시 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | JWT 서명 | high | mitigate | `JWT_SECRET` 32바이트+ 환경변수, `passport-jwt` 표준 검증, 24시간 만료 |
| T-02-02 | Tampering | business_reg_no 저장 | high | mitigate | AES-256-GCM(태그로 무결성 보장) — 02-01 checkpoint 확정값 반영 |
| T-02-03 | Information Disclosure | business_reg_no_digest 유출 후 역산 | high | mitigate | 단순 SHA-256 금지, `HMAC-SHA256(bizNo, pepper)`만 사용 (db-schema-design.md §민감정보 저장 규칙) |
| T-02-04 | Information Disclosure | password_hash 유출 후 역산 | high | mitigate | `crypto.scrypt`(N=16384 이상, 사용자별 랜덤 salt) |
| T-02-05 | Denial of Service | 회원가입 엔드포인트 스팸 | medium | accept | MVP 범위에서 rate limiting 보류 — 남용 관찰 후 02 이후 phase에서 대응, 지금은 UNIQUE 제약이 최소 방어선 |
</threat_model>

<verification>
- `npm run test:e2e --workspace=apps/api -- tracer.e2e-spec.ts` 통과
- 로그인 자체의 단위테스트는 02-03 범위(auth.controller.spec.ts)
</verification>

<success_criteria>
회원가입(PROF-05 절반, 암호화 저장), 업종코드 등록 즉시 매칭(MATCH-01 최초 버전), 이메일 알림 기록까지 실코드로 동작하며, 이후 5개 플랜(02-03~02-07)이 상속할 인증·매칭·알림 기반이 고정된다.
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-02-SUMMARY.md` when done
</output>
