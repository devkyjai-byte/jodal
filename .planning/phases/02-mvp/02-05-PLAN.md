---
phase: 02-mvp
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/api/src/queues/queues.module.ts
  - apps/api/src/queues/ingest.processor.ts
  - apps/api/src/queues/match.processor.ts
  - apps/api/src/announcements/announcements.module.ts
  - apps/api/src/announcements/announcements.service.ts
  - apps/api/src/announcements/ports/announcement-source.port.ts
  - apps/api/src/announcements/adapters/fixture-announcement-source.adapter.ts
  - apps/api/src/announcements/adapters/g2b-announcement-source.adapter.ts
  - apps/api/src/matching/matching.service.ts
  - apps/api/tests/fixtures/announcements.revision.sample.json
  - apps/api/tests/fixtures/announcements.bulk.sample.json
  - .env.example
autonomous: true
requirements: [ING-01, ING-02, ING-03, MATCH-01]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "시스템은 나라장터 API 승인 여부와 무관하게 지금 픽스처 데이터로 배치 수집을 실행할 수 있고, 실제 API 연동은 코드 재작성 없이 어댑터 교체만으로 활성화된다"
    - "동일 공고가 개정되어(source_revision_no 증가) 다시 들어오면 새 행이 추가되고 이전 차수의 is_latest_revision이 false로 갱신된다 (기존 matches 행은 삭제되지 않는다)"
    - "신규/갱신된 공고가 수집되면 그 공고에 대해 등록된 prefix가 일치하는 모든 업체의 matches가 자동으로 재계산된다 (수동 트리거 불필요)"
    - "prefix 매칭 조회가 인덱스를 사용한다는 것이 EXPLAIN ANALYZE로 확인되었다"
  artifacts:
    - path: "apps/api/src/announcements/ports/announcement-source.port.ts"
      provides: "수집 소스 어댑터 인터페이스 (fetchLatest)"
      exports: ["AnnouncementSourcePort"]
    - path: "apps/api/src/announcements/adapters/g2b-announcement-source.adapter.ts"
      provides: "실제 나라장터 API 호출 어댑터 (기본 비활성, ANNOUNCEMENT_SOURCE=g2b로 전환)"
    - path: "apps/api/src/queues/ingest.processor.ts"
      provides: "BullMQ upsertJobScheduler 기반 4~6회/일 배치 수집 잡"
    - path: "apps/api/src/queues/match.processor.ts"
      provides: "신규 공고 배치에 대한 팬아웃 재매칭 워커"
  key_links:
    - from: "apps/api/src/queues/ingest.processor.ts"
      to: "apps/api/src/announcements/announcements.service.ts"
      via: "ingestService.pollAndUpsert()"
      pattern: "pollAndUpsert"
    - from: "apps/api/src/queues/ingest.processor.ts"
      to: "apps/api/src/queues/match.processor.ts"
      via: "matchQueue.add('compute-matches', { announcementIds })"
      pattern: "compute-matches"
    - from: "apps/api/src/queues/match.processor.ts"
      to: "apps/api/src/queues/queues.module.ts (notify queue)"
      via: "notifyQueue.add('dispatch-notifications', { matchIds })"
      pattern: "dispatch-notifications"
---

<objective>
02-02의 `matching.service.ts`(업체 1곳 등록 시 동기 재계산)를 실제 배치 파이프라인으로 확장한다: 픽스처 기반 수집(ING-01, 나라장터 API 승인 대기 중에도 지금 동작), 정규화(ING-02), 중복·개정 병합(ING-03), 그리고 신규 공고에 대한 전체 업체 팬아웃 재매칭(MATCH-01 완성)까지 RESEARCH.md Pattern 1(수집→매칭→발송 BullMQ 체인)을 그대로 구현한다.

`AnnouncementSourcePort` 인터페이스로 수집원을 추상화해, 실제 나라장터 API가 승인된 뒤에는 `ANNOUNCEMENT_SOURCE=g2b` 환경변수 전환만으로 활성화되고 코드 재작성이 없다 — critical_constraint의 핵심 요구사항이다.

Purpose: 시드/픽스처로 지금 당장 매칭 파이프라인 전체를 검증 가능하게 하고, 실제 공고 데이터가 흘러들어올 준비를 미리 끝낸다.
Output: 배치 수집→정규화→병합→팬아웃 재매칭까지 동작하는 BullMQ 파이프라인, EXPLAIN ANALYZE로 검증된 인덱스.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design/db-schema-design.md
@docs/design/업종-물품분류-매핑.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-01-SUMMARY.md
@.planning/phases/02-mvp/02-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>배치 수집 파이프라인 — 픽스처 어댑터 + 정규화 + 중복·개정 병합 end-to-end</name>
  <precondition>02-01의 Prisma 스키마(bid_announcements, matches 테이블)와 Redis(docker-compose), 02-02의 matching.service.ts(scoreMatch/toQualitativeTier)가 존재한다</precondition>
  <read_first>docs/design/db-schema-design.md (§데이터 흐름 스파인 ②, §복수성·병합 규칙 (b), §Phase 2 인계 사항 3), .planning/phases/02-mvp/02-RESEARCH.md (§Architecture Patterns Pattern 1, §Common Pitfalls Pitfall 3·4, §Standard Stack BullMQ/ioredis 버전)</read_first>
  <files>apps/api/src/queues/queues.module.ts, apps/api/src/queues/ingest.processor.ts, apps/api/src/announcements/announcements.module.ts, apps/api/src/announcements/announcements.service.ts, apps/api/src/announcements/ports/announcement-source.port.ts, apps/api/src/announcements/adapters/fixture-announcement-source.adapter.ts, apps/api/src/announcements/adapters/g2b-announcement-source.adapter.ts, apps/api/tests/fixtures/announcements.revision.sample.json, .env.example</files>
  <action>
    `bullmq@6.2.2`, `ioredis@6.0.0`, `@nestjs/bullmq@11.0.5`를 정확히 이 버전으로 설치한다(RESEARCH.md §Standard Stack — Package Legitimacy Audit에서 이미 승인됨, T-02-SC 참고). `queues.module.ts`에서 `ingest`/`match`/`notify` 3개 큐를 `BullModule.registerQueue`로 한 번에 등록해 다른 모듈(matching, notifications)이 재등록 없이 가져다 쓸 수 있게 한다(02-07가 `notify` 큐의 컨슈머만 추가하고 이 모듈은 수정하지 않는다).

    `AnnouncementSourcePort`(`fetchLatest(): Promise<RawAnnouncement[]>`) 인터페이스를 정의하고 두 구현체를 만든다: `FixtureAnnouncementSourceAdapter`(`apps/api/tests/fixtures/*.json`을 읽어 반환, `ANNOUNCEMENT_SOURCE` 환경변수 기본값이자 나라장터 API 미승인 상태의 현재 활성 구현)와 `G2BAnnouncementSourceAdapter`(Node 내장 `fetch`로 실제 data.go.kr 나라장터 입찰공고정보서비스를 호출하는 완전한 프로덕션 코드 — `NARAJANGTEO_API_KEY` 환경변수 사용, 실제 승인 후 `ANNOUNCEMENT_SOURCE=g2b`로 전환만 하면 활성화된다. API 응답 필드명은 `.planning/phases/01-prep/01-VERIFICATION.md`가 `human_needed`로 남긴 미확정 항목이므로, 이 어댑터는 응답 파싱 실패를 개별 레코드 단위로 잡아 로그만 남기고 나머지 레코드 처리를 막지 않는다). `NestJS AppModule`에 두 어댑터 모두 provider로 등록하고 `ANNOUNCEMENT_SOURCE` 값에 따라 `AnnouncementSourcePort` 토큰에 주입할 구현체를 런타임 선택한다(팩토리 프로바이더).

    `AnnouncementsService.pollAndUpsert()`: 활성 소스에서 받은 원문을 파싱해 `classification_code`(품목분류번호, 실패 시 NULL 허용 — db-schema-design.md §스파인이 강제하는 설계 제약 (c)), `region_codes`, `budget_amount`, `bid_open_at`/`bid_close_at`을 정규화하고 원문 전체를 `raw_payload`(JSONB)에 보관한다(ING-02). `(source_bid_no, source_revision_no)` 복합키로 UPSERT하되, 같은 `source_bid_no`에 더 큰 `source_revision_no`가 들어오면 이전 차수 행의 `is_latest_revision`을 `false`로 갱신하고 새 행을 `is_latest_revision=true`로 삽입한다 — 기존 `matches` 행은 삭제하지 않는다(ING-03, db-schema-design.md §복수성·병합 규칙 (b)). `source_revision_no` 필드가 실제 나라장터 응답에서 어떻게 채워지는지 검증되지 않았으므로(§Phase 2 인계 사항 3), 빈 문자열/누락/숫자 세 가지 입력 형태 모두를 `'0'` 기본값으로 정규화하는 방어 로직을 넣는다.

    `queues/ingest.processor.ts`: `upsertJobScheduler('poll-g2b', { pattern: process.env.INGEST_CRON_PATTERN ?? '0 0 */4 * * *' }, ...)`로 4시간마다(하루 6회) 실행되는 리피터블 잡을 등록한다 — 크론 표현식을 환경변수로 뺀 이유는 나라장터 실제 일일 한도가 아직 미확인(RESEARCH.md Pitfall 3, Open Question 1)이라 승인 후 재조정이 필요하기 때문이다. 잡 완료 후 새로 upsert된 `announcementIds`가 있으면 `matchQueue.add('compute-matches', { announcementIds })`를 호출한다(Pattern 1).

    `apps/api/tests/fixtures/announcements.revision.sample.json`에 동일 `source_bid_no`, `source_revision_no`가 `'0'`→`'1'`로 바뀐 2개 버전의 공고를 만들어 개정 병합을 검증할 픽스처로 쓴다.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- announcements.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ANNOUNCEMENT_SOURCE=fixture`(기본값)로 `pollAndUpsert()` 실행 시 픽스처의 공고가 모두 `bid_announcements`에 적재된다
    - 개정 픽스처를 두 번(차수 0, 차수 1) 연속 수집하면 차수 0 행의 `is_latest_revision=false`, 차수 1 행이 신규 삽입되고 `is_latest_revision=true`, 총 2개 행이 존재한다(삭제되지 않음)
    - `classification_code`가 없는(null) 원문도 예외 없이 적재된다
    - `G2BAnnouncementSourceAdapter`는 코드가 존재하고 컴파일되지만, `ANNOUNCEMENT_SOURCE=g2b`로 전환하지 않는 한 호출되지 않는다(단위테스트에서 fetch 모킹으로 어댑터 자체 로직만 별도 검증)
  </acceptance_criteria>
  <done>픽스처 기반 배치 수집·정규화·개정 병합이 실제로 동작하고, 실 API 전환은 환경변수 하나로 가능한 상태다.</done>
</task>

<task type="auto">
  <name>팬아웃 재매칭 워커 + prefix 인덱스 실측 검증</name>
  <read_first>docs/design/db-schema-design.md (§인덱스와 조회 패턴, §Phase 2 인계 사항 2), .planning/phases/02-mvp/02-RESEARCH.md (§Code Examples MATCH-01 스코어링 함수)</read_first>
  <files>apps/api/src/queues/match.processor.ts, apps/api/src/matching/matching.service.ts, apps/api/tests/fixtures/announcements.bulk.sample.json</files>
  <action>
    `matching.service.ts`에 `scoreAndUpsertForAnnouncements(announcementIds: string[])`를 추가한다: 각 공고의 `classification_code`에 대해, 그 prefix로 시작될 수 있는 모든 등록 prefix를 가진 업체를 `company_classification_codes`에서 리터럴 OR절로 조회(db-schema-design.md §스파인이 강제하는 설계 제약 (b) 권장안 그대로 재사용 — 02-02의 단일 업체 버전과 동일한 `scoreMatch`/`toQualitativeTier` 함수를 재사용해 아키텍처를 바꾸지 않는다)해 각 (업체, 공고) 쌍의 `matches`를 UPSERT한다. `classification_code`가 NULL인 공고는 매칭에서 완전히 제외하지 않고 고정 낮은 점수(20점, RESEARCH.md 스코어링 함수 기준)로 후보에 남긴다.

    `queues/match.processor.ts`(`@Processor('match')`)가 `compute-matches` 잡을 받아 `scoreAndUpsertForAnnouncements`를 호출하고, 새로 생성/갱신된 `matches` 중 `min_score_threshold` 이상인 것의 `matchIds`를 모아 `notifyQueue.add('dispatch-notifications', { matchIds })`를 호출한다(Pattern 1 — 이 큐의 컨슈머는 02-07가 구현).

    `apps/api/tests/fixtures/announcements.bulk.sample.json`에 최소 200건의 합성 공고(classification_code를 43/44/55/72(임의) 대분류에 고르게 분산, region_codes 다양화)를 생성하는 시드 스크립트를 만들고, 이를 적재한 뒤 `EXPLAIN ANALYZE`로 `classification_code LIKE '43%'` 조회가 `idx_bid_announcements_classification_code_pattern` 인덱스(Index Scan/Bitmap Index Scan)를 타는지 확인한다(db-schema-design.md §Phase 2 인계 사항 2). Seq Scan이 나오면 원인(통계 미갱신 `ANALYZE` 누락 등)을 조사해 해결하거나, 해결 불가 시 SUMMARY에 관측 결과와 대안(파생 prefix 테이블, §스파인이 강제하는 설계 제약 (b)-(ii))을 기록한다 — 이 결정을 코드 변경 없이 뒤집을 수 있는 범위이므로 checkpoint는 불필요하다(reversible). 이 인덱스 실측 결과는 02-06(피드 화면)의 `region_codes` GIN 인덱스 보류 판단과 함께 Phase 2 이후 백로그 항목("인덱스 튜닝 재검토")으로 묶어 SUMMARY에 남긴다 — 두 판단이 지금까지 서로 독립적으로 각 플랜에만 남아 있었다.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- matching.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 신규 공고 1건(classification `43211501`)을 수집한 뒤 `43` 또는 `4321`을 등록한 모든 업체에 대해 `matches` 행이 생성된다(0건이 아니라 정확히 그 조건을 만족하는 업체 수만큼)
    - `classification_code`가 NULL인 공고도 `matches`에 점수 20으로 후보로 남는다(완전 배제되지 않음)
    - EXPLAIN ANALYZE 결과가 SUMMARY.md에 원문 그대로 기록된다(Index Scan 여부와 관계없이)
  </acceptance_criteria>
  <done>배치 수집으로 들어온 신규 공고가 전체 업체 대상으로 자동 재매칭되고, 인덱스 사용 여부가 실측 확인되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 나라장터 Open API(외부, 향후 활성화) → NestJS 워커 | 신뢰할 수 없는 원문 JSON/XML, 파싱 오류 가능성 |
| BullMQ 워커 ↔ Redis | 잡 페이로드에 공고 ID만 담고 민감정보 없음 |
| bid_announcements.raw_payload → 사용자 화면 | 파싱 결과가 원문과 다를 수 있음(02-06 상세 화면의 원문 링크로 완화) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | 원문 파싱 결과와 실제 나라장터 공고 불일치 | high | mitigate | `raw_payload` 원문 보관(재파싱 가능) + 02-06 상세 화면에 나라장터 원문 링크 필수 배치(db-schema-design.md §Phase 2 인계 사항 6, T-01-15) — 이 플랜은 저장까지만 책임 |
| T-02-10 | Denial of Service | 나라장터 API 일일 호출 한도 초과로 인한 IP/키 차단 | high | mitigate | 크론 주기를 환경변수화(`INGEST_CRON_PATTERN`)해 실제 한도 확인 후 즉시 조정 가능, 기본값은 보수적인 4시간(6회/일) |
| T-02-11 | Information Disclosure | G2B/국세청 API 키가 로그에 노출 | medium | mitigate | `raw_payload` 로깅 시 요청 헤더·쿼리스트링의 서비스키 필드를 마스킹 |
| T-02-SC | Tampering | npm install(bullmq/ioredis/@nestjs/bullmq) | high | accept | RESEARCH.md Package Legitimacy Audit에서 이미 재검증·승인됨(오탐 9건 중 3건) — 추가 체크포인트 불필요 |
</threat_model>

<verification>
- `npm run test --workspace=apps/api` 전체 통과 (announcements.service.spec.ts, matching.service.spec.ts 포함)
- 200건 벌크 픽스처 적재 후 EXPLAIN ANALYZE 결과가 SUMMARY.md에 기록됨
</verification>

<success_criteria>
ING-01~03과 MATCH-01이 배치 파이프라인으로 완결되며, 실제 API 전환이 어댑터 교체만으로 가능한 상태다.
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-05-SUMMARY.md` when done
</output>
