---
phase: 02-mvp
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - docker-compose.yml
  - .env.example
  - apps/api/src/search/search.module.ts
  - apps/api/src/search/meilisearch.service.ts
  - apps/api/src/announcements/announcements.service.ts
  - apps/api/src/announcements/announcements.controller.ts
  - apps/api/src/announcements/dto/search-query.dto.ts
  - apps/web/app/feed/page.tsx
  - apps/web/components/feed/FilterBar.tsx
  - apps/web/components/feed/AnnouncementCard.tsx
  - apps/web/app/announcements/[id]/page.tsx
  - apps/web/public/sw.js
autonomous: false
requirements: [ING-04, MATCH-01, CLIENT-01]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "로그인한 업체는 키워드·업종·지역·마감일로 자신의 매칭 공고를 검색·필터링할 수 있다"
    - "피드 카드는 적합도를 5단계 정성 등급으로만 표시하고, 원점수·백분율은 어디에도 노출되지 않는다"
    - "공고 상세 화면에는 나라장터 원문 링크가 스크롤 없이 보이는 위치에 항상 존재한다"
    - "다른 업체의 match_id로 상세에 접근하면 403이 반환된다"
    - "네트워크가 끊겨도 마지막으로 불러온 피드가 캐시에서 표시된다"
  artifacts:
    - path: "apps/api/src/search/meilisearch.service.ts"
      provides: "공고 색인·검색 클라이언트 래퍼"
    - path: "apps/api/src/announcements/announcements.controller.ts"
      provides: "GET /feed (검색+필터), GET /announcements/:id (상세, 소유권 검증)"
    - path: "apps/web/app/feed/page.tsx"
      provides: "공고 피드 화면 (ING-04, CLIENT-01)"
    - path: "apps/web/app/announcements/[id]/page.tsx"
      provides: "공고 상세 화면 (CLIENT-01)"
  key_links:
    - from: "apps/api/src/announcements/announcements.service.ts (pollAndUpsert)"
      to: "apps/api/src/search/meilisearch.service.ts"
      via: "upsert 직후 indexAnnouncement(record) 호출"
      pattern: "indexAnnouncement"
    - from: "apps/web/app/feed/page.tsx"
      to: "apps/api/src/announcements/announcements.controller.ts"
      via: "GET /feed?keyword&classification&region&deadline (JWT)"
      pattern: "/feed"
    - from: "apps/web/app/announcements/[id]/page.tsx"
      to: "apps/api/src/announcements/announcements.controller.ts"
      via: "GET /announcements/:id, matches 소유권 검증"
      pattern: "announcements/:id"
---

<objective>
02-04가 채워 넣은 `bid_announcements`/`matches`를 실제로 "볼 수 있게" 만든다: 검색엔진 색인+검색(ING-04), 공고 피드 화면, 공고 상세 화면(CLIENT-01)까지 `02-feed.md`·`03-detail.md`가 서술한 레이아웃·상호작용·엣지 케이스를 그대로 구현한다. 매칭 원점수는 API 응답에서도 UI에서도 절대 노출하지 않는다(Legal 제약).

Purpose: 사용자가 "받고 싶은 조달을 놓치지 않는다"는 핵심 가치를 실제로 체감하는 화면.
Output: 검색·필터가 동작하는 피드 화면 + 나라장터 원문 대조가 가능한 상세 화면.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design/wireframes/02-feed.md
@docs/design/wireframes/03-detail.md
@docs/design/db-schema-design.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>검색엔진: Meilisearch vs OpenSearch</decision>
  <context>
    `docs/design/wireframes/02-feed.md` §데이터 소스가 "어느 쪽을 쓸지 최종 선택은 Phase 2로 위임한다"고 명시했다. RESEARCH.md는 Meilisearch를 권장(단일 바이너리·JVM 불필요·1인 운영 부담 최소)하지만, 아직 사람이 확정한 적은 없다. 색인에 데이터가 쌓인 뒤 엔진을 바꾸려면 전체 재색인 + 클라이언트 코드 교체가 필요하다(one-way).
  </context>
  <options>
    <option id="meilisearch">
      <name>Meilisearch (RESEARCH 권장)</name>
      <pros>단일 바이너리, 설정 없이 즉시 구동, 1인 운영 부담 최소, npm 클라이언트 0.60.0 승인됨</pros>
      <cons>대규모(수천만 건) 로그성 데이터에는 OpenSearch보다 약함 — 이 프로젝트 초기 공고량(연 수만 건 추정)에는 과설계 아님</cons>
    </option>
    <option id="opensearch">
      <name>OpenSearch</name>
      <pros>대규모 확장성, Elasticsearch 생태계 호환</pros>
      <cons>JVM 힙 튜닝·샤드 관리 필요 — 1인 개발 운영 부담 큼, MVP 단계 데이터량에 과설계</cons>
    </option>
  </options>
  <resume-signal>Select: meilisearch 또는 opensearch (미응답/기본값은 meilisearch)</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>검색 색인·API + 공고 피드 화면 end-to-end</name>
  <precondition>02-04의 announcements.service.ts와 bid_announcements 시드 데이터가 존재한다</precondition>
  <reversibility rating="one-way">검색엔진 선택은 위 checkpoint가 확정했다 — 이 태스크는 그 결정을 구현한다.</reversibility>
  <read_first>docs/design/wireframes/02-feed.md (전체), .planning/phases/02-mvp/02-RESEARCH.md (§Don't Hand-Roll 전문검색, §Standard Stack meilisearch 버전)</read_first>
  <files>docker-compose.yml, .env.example, apps/api/src/search/search.module.ts, apps/api/src/search/meilisearch.service.ts, apps/api/src/announcements/announcements.service.ts, apps/api/src/announcements/announcements.controller.ts, apps/api/src/announcements/dto/search-query.dto.ts, apps/web/app/feed/page.tsx, apps/web/components/feed/FilterBar.tsx, apps/web/components/feed/AnnouncementCard.tsx, apps/web/public/sw.js</files>
  <action>
    `docker-compose.yml`에 위 checkpoint 확정값의 이미지(`getmeili/meilisearch:v1` 등)를 추가하고 `.env.example`에 마스터키 자리표시자를 기록한다. `meilisearch@0.60.0`(RESEARCH.md 승인됨) JS 클라이언트를 설치한다. `meilisearch.service.ts`에 `indexAnnouncement(record)`(title/agency_name/classification_code/region_codes/bid_close_at 필드를 색인)와 `searchAnnouncementIds(keyword, filters): Promise<string[]>`를 구현한다. `announcements.service.ts`의 `pollAndUpsert()` UPSERT 직후(02-04가 만든 지점)에 `indexAnnouncement()` 호출을 추가해 수집과 색인이 항상 동기화되게 한다.

    `GET /feed`(JWT 가드, 쿼리: `keyword?`, `classification[]?`, `region[]?`, `deadline?(this_week|this_month)`, `sort?(score|deadline|latest, 기본 score)`)를 구현한다: `matches.company_id = 로그인 업체`로 항상 스코프하고(db-schema-design.md §스파인 조인 SQL, T-01-12), `keyword`가 있으면 먼저 Meilisearch로 후보 `announcement_id` 집합을 얻어 `matches` 조인에 `IN` 절로 교집합을 취한다. 응답 DTO는 `qualitativeTier`(5단계 문자열)만 포함하고 원점수 필드를 아예 직렬화하지 않는다(RESEARCH.md Anti-Pattern "매칭 스코어를 사용자에게 숫자·백분율로 노출" 금지 — 필드 자체를 제거해 실수로도 흘려보내지 않는다). 마감 지난 공고는 기본 제외하고 `includeExpired=true` 쿼리로만 노출한다(02-feed.md §엣지 케이스). `region[]` 필터는 db-schema-design.md §Phase 2 인계 사항 7이 결정을 보류한 `region_codes` 배열 컬럼을 `= ANY(region_codes)`로 조회한다 — GIN 인덱스는 지금 추가하지 않는다(초기 데이터량에서 체감 영향이 작다는 그 문서의 판단을 그대로 따름); 이 조회가 실측으로 느려지면(예: 벌크 픽스처 200건 기준 EXPLAIN ANALYZE에서 Seq Scan + 느린 실행시간 관측) GIN 인덱스 추가를 후속 작업으로 남긴다(SUMMARY에 관측 결과 기록).

    `apps/web/app/feed/page.tsx`: `FilterBar.tsx`(키워드 입력+Enter/검색아이콘, 업종 다중 드롭다운, 지역 다중 선택, 마감일 범위, "필터 초기화")와 정렬 드롭다운(적합도순 기본)을 필터·정렬 상태를 쿼리스트링에 반영하며 구현한다(무한 스크롤, 디바운스 적용). `AnnouncementCard.tsx`: 공고명·발주기관·마감 D-day·예산금액·5단계 정성 등급 배지("매우 적합"~"참고용", 절대 숫자 없음, 카드 하단에 "참고용 정보" 한 줄 고지)·매칭 근거 한 줄("정보통신(43) 업종 등록과 일치")을 렌더링한다. 업종 미등록(매칭 0건, 프로필 미완성) 상태와 매칭 0건(프로필은 있으나 결과 없음)을 구분해 각각 다른 안내 문구+온보딩 복귀 링크를 표시한다(02-feed.md §엣지 케이스, 두 경우를 하나로 뭉뚱그리지 않는다). 로딩 스켈레톤을 구현한다.

    `apps/web/public/sw.js`에 `GET /feed` 요청을 가로채는 fetch 이벤트 핸들러를 추가한다: 온라인이면 네트워크 우선 + 성공 응답을 Cache Storage에 저장, 네트워크 실패 시 캐시된 마지막 응답을 반환한다. 프론트 페이지가 SW로부터 캐시 응답임을 알 수 있도록 응답 헤더 또는 별도 플래그를 확인해 "오프라인 상태 — 마지막 업데이트 기준" 배지를 표시하고, 캐시조차 없으면 재시도 버튼을 노출한다(02-feed.md §엣지 케이스). 신규 npm 패키지(next-pwa 등)는 추가하지 않는다 — 브라우저 내장 Service Worker/Cache API만 사용한다.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- announcements.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GET /feed?keyword=컴퓨터`가 Meilisearch 색인을 거쳐 제목에 "컴퓨터"가 포함된 매칭 공고만 반환한다
    - `GET /feed` 응답 JSON 어디에도 `score`/원점수 숫자 필드가 없다(테스트가 응답 바디 키 목록을 검사)
    - `region=서울특별시` 필터 시 해당 지역 미포함 공고가 결과에서 빠진다
    - 다른 업체의 JWT로 조회해도 자신의 `matches` 범위만 반환된다(교차 노출 없음)
  </acceptance_criteria>
  <done>키워드·업종·지역·마감일 검색이 실제로 동작하고, 피드 화면이 등급만 노출하며, 오프라인 캐시 폴백이 동작한다.</done>
</task>

<task type="auto">
  <name>공고 상세 화면 — 원문 링크·매칭 근거·엣지 케이스</name>
  <read_first>docs/design/wireframes/03-detail.md (전체), docs/design/db-schema-design.md (§Phase 2 인계 사항 6, T-01-15·T-01-16)</read_first>
  <files>apps/api/src/announcements/announcements.controller.ts, apps/web/app/announcements/[id]/page.tsx</files>
  <action>
    `GET /announcements/:id`(JWT 가드)를 추가한다: `bid_announcements`를 id로 조회하고, `matches`에서 `company_id = 로그인 업체 AND announcement_id = :id`로 매칭 근거(일치한 prefix, 자릿수, 지역 일치 여부)를 조회한다. 요청에 `match_id` 쿼리 파라미터가 있으면(이메일 링크 경로) 그 `matches.company_id`가 로그인 업체와 다를 경우 403을 반환한다(03-detail.md §엣지 케이스 "다른 업체의 매칭 식별자로 접근 시도", T-01-16). `raw_payload` 전체는 응답에 포함하지 않고 정규화된 필드(핵심 요약, 참가자격 발췌 텍스트)만 반환한다.

    `apps/web/app/announcements/[id]/page.tsx`: 헤더(공고명·나라장터 공고번호+개정차수·발주기관·마감 D-day) 바로 아래 above-the-fold 위치에 나라장터 원문 링크 버튼을 고정 배치한다(필수 요소, T-01-15). 핵심 요약표, 참가자격 원문 발췌(접힘 기본, "더 보기"로 펼침 — 해석·판정 문구 절대 추가하지 않음), 매칭 근거 영역("자세히"로 펼침), 하단 액션(원문에서 입찰 참여하기/이 공고 저장(로컬스토리지 토글+토스트, db-schema-design.md가 저장 테이블 설계를 Phase 2로 위임했고 이 화면 문서도 저장 대상을 확정하지 않았으므로 서버 테이블 없이 클라이언트 로컬 저장으로 이번 Phase 범위를 충족한다)/이 업종 알림 조정하기 → `/settings/notifications?from=detail&hint={prefix}`), Legal 고지 문구("조달메이트는 정보 제공·보조 도구이며...")를 고정 배치한다. 마감된 공고("마감됨" 배지, 열람은 유지), 개정된 공고("최신 공고 보기" 배너), 파싱 실패 필드("정보 없음" 표시 + 원문 링크 강조), 취소·삭제된 공고(상세 렌더링 안 함, 안내만) 4가지 엣지 케이스를 모두 구현한다.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- announcements.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 다른 업체 소유의 `match_id`로 상세 조회 시 403
    - 마감·개정·파싱실패·취소 4가지 시드 데이터 각각에 대해 해당 엣지 케이스 UI 분기가 렌더링된다(수동 확인 후 SUMMARY 기록)
    - 나라장터 원문 링크가 페이지 스크롤 없이 뷰포트 안에 존재한다(수동 확인)
  </acceptance_criteria>
  <done>공고 상세가 원문 대조 가능한 형태로 완성되고, 소유권 검증·4개 엣지 케이스가 모두 동작한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /feed, /announcements/:id | JWT로 인증되었으나 company_id 스코프 강제는 서버 책임 |
| NestJS API → Meilisearch | 공고 데이터는 업체별 민감정보가 아니므로 색인 자체는 공개 데이터 취급 가능하나, 클라이언트에서 직접 Meilisearch를 호출하지 않고 반드시 API를 경유한다(마스터키 비노출) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Elevation of Privilege | 다른 업체의 match_id로 상세 접근 | high | mitigate | `matches.company_id` 소유권 검증 후 불일치 시 403 (db-schema-design.md T-01-16) |
| T-02-13 | Information Disclosure | GET /feed 응답에 원점수 노출 | medium | mitigate | DTO에서 score 필드를 직렬화 자체를 하지 않음(등급 문자열만 포함) — Legal 제약 직결 |
| T-02-14 | Tampering | Meilisearch 마스터키가 프론트에 노출 | high | mitigate | 프론트는 Meilisearch를 직접 호출하지 않고 NestJS API만 경유, 마스터키는 서버 환경변수에만 존재 |
| T-02-SC | Tampering | npm install(meilisearch 또는 opensearch 클라이언트) | high | accept | RESEARCH.md Package Legitimacy Audit에서 이미 승인됨(OK 판정) — 추가 체크포인트 불필요 |
</threat_model>

<verification>
- `npm run test --workspace=apps/api -- announcements.controller.spec.ts` 통과
- `GET /feed` 응답 스키마에 score 필드 부재 확인(테스트로 자동화)
- 4개 상세 엣지 케이스 수동 확인 후 SUMMARY.md 기록
</verification>

<success_criteria>
ING-04(검색·필터)와 CLIENT-01(피드·상세 화면)이 실제 데이터로 완성되고, Legal 제약(등급만 노출)이 API 레벨에서 강제된다.
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-05-SUMMARY.md` when done
</output>
