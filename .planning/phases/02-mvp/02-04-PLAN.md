---
phase: 02-mvp
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/api/src/companies/classification-codes.controller.ts
  - apps/api/src/companies/companies.controller.ts
  - apps/api/src/companies/performances.controller.ts
  - apps/api/src/companies/certifications.controller.ts
  - apps/api/src/companies/companies.service.ts
  - apps/api/src/companies/dto/
  - apps/web/lib/classification-tree.data.ts
  - apps/web/app/onboarding/page.tsx
  - apps/web/components/onboarding/ClassificationStep.tsx
  - apps/web/components/onboarding/RegionStep.tsx
  - apps/web/components/onboarding/PerformanceCertStep.tsx
  - apps/web/components/onboarding/NotificationInitialStep.tsx
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, CLIENT-01]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "업체는 물품분류 대분류/중분류를 여러 개 선택해 등록·조회·삭제할 수 있다 (2/4/6/8자리 모두 허용)"
    - "업체는 활동 지역(시/도)을 여러 개 등록할 수 있다"
    - "업체는 과거 실적과 보유 인증을 등록할 수 있고, 건너뛰어도 가입이 완료된다"
    - "온보딩 스텝 2~5가 실제 API(classification-codes 목록 조회 + GET /companies/me)에 연결되어 새로고침 후에도 등록한 값이 유지된다"
    - "시설관리 업종은 분류코드가 아직 미확정임이 화면에 '코드 확인 중'으로 표시되고, 사용자가 직접 코드를 입력해 선택할 수 있다"
    - "로그인한 업체가 GET /companies/me를 호출하면 regionCodes·classificationCodes·프로필 완성 여부를 한 번에 조회할 수 있다"
  artifacts:
    - path: "apps/api/src/companies/classification-codes.controller.ts"
      provides: "다중 분류코드 등록/조회/삭제 (PROF-01)"
    - path: "apps/api/src/companies/companies.controller.ts"
      provides: "GET /companies/me (프로필 읽기 — regionCodes/classificationCodes/profileComplete), PATCH /companies/me (지역 갱신, PROF-02)"
    - path: "apps/web/app/onboarding/page.tsx"
      provides: "5스텝 온보딩 흐름 (스텝 2~5 wiring)"
    - path: "apps/web/lib/classification-tree.data.ts"
      provides: "목표 업종 4개(43/44/55/시설관리 확인중) 추천 트리 데이터"
  key_links:
    - from: "apps/web/components/onboarding/ClassificationStep.tsx"
      to: "apps/api/src/companies/classification-codes.controller.ts"
      via: "POST/GET/DELETE /companies/me/classification-codes"
      pattern: "classification-codes"
    - from: "apps/web/components/onboarding/RegionStep.tsx"
      to: "apps/api/src/companies/companies.controller.ts"
      via: "PATCH /companies/me { regionCodes } 저장, GET /companies/me 재조회"
      pattern: "regionCodes"
---

<objective>
02-02가 만든 signup 직후 상태(계정 1개, 분류코드 없음)에서 온보딩을 실제로 완주할 수 있는 상태로 확장한다: 업종(다중), 지역(다중), 실적·인증(선택)까지 등록해 `01-onboarding.md`가 서술한 5스텝 전체가 실제 API로 동작하게 한다. PROF-01~04를 이 플랜에서 완결한다.

이 플랜은 또한 `GET /companies/me` 읽기 엔드포인트를 새로 추가한다 — gsd-plan-checker가 지적한 gap으로, 이전까지는 `PATCH /companies/me`만 존재해 지역 등록 후 재조회할 방법이 없었고(01-onboarding.md가 요구하는 "새로고침 시 재조회" 동작에 응답할 읽기 엔드포인트가 없는 실제 기능 공백이었다), `RegionStep.tsx`도 재조회를 호출하지 않고 있었다. 이 플랜은 엔드포인트와 프론트 재조회 호출을 함께 완성한다.

Purpose: 프로필이 완성되어야 매칭(MATCH-01)이 의미 있는 범위로 동작한다 — 02-02의 매칭은 분류코드 1개만 가정한 최소 경로였다.
Output: 온보딩 스텝 2~5 UI + 대응 API 엔드포인트(GET /companies/me 포함), 새로고침 후에도 유지되는 프로필 데이터.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design/wireframes/01-onboarding.md
@docs/design/업종-물품분류-매핑.md
@.planning/phases/02-mvp/02-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>업종(다중)·지역 등록 — 온보딩 스텝 2~3 end-to-end</name>
  <precondition>02-02의 POST /companies/me/classification-codes와 JWT 인증 미들웨어가 동작한다 (02-02 tracer.e2e-spec.ts green)</precondition>
  <read_first>docs/design/wireframes/01-onboarding.md (스텝 2·3, §상호작용, §데이터 소스), docs/design/업종-물품분류-매핑.md (§목표 업종 매핑표, §미확인 항목과 확인 방법 1번, §프로필 저장 형식 권장안)</read_first>
  <files>apps/api/src/companies/classification-codes.controller.ts, apps/api/src/companies/companies.controller.ts, apps/api/src/companies/companies.service.ts, apps/web/lib/classification-tree.data.ts, apps/web/app/onboarding/page.tsx, apps/web/components/onboarding/ClassificationStep.tsx, apps/web/components/onboarding/RegionStep.tsx</files>
  <action>
    `classification-codes.controller.ts`에 `GET /companies/me/classification-codes`(목록)와 `DELETE /companies/me/classification-codes/:id`(소유권 검증 — 다른 업체 행 삭제 시도는 403)를 추가한다. `POST`는 02-02에 이미 있으나 이제 "업종을 1개도 등록하지 않은 채 다음으로 진행 불가" 검증을 프론트에서 강제할 수 있도록 목록 응답을 프론트가 신뢰할 수 있게 정리한다.

    `companies.controller.ts`에 `PATCH /companies/me`(현재는 `regionCodes: string[]`만 받음, JWT 가드)를 추가해 `companies.region_codes` 배열 컬럼을 갱신한다(PROF-02, db-schema-design.md §복수성·병합 규칙 (a)).

    같은 컨트롤러에 `GET /companies/me`(JWT 가드)를 추가한다: 로그인 업체의 `companies` 행을 조회해 `id`, `companyName`, `contactEmail`, `regionCodes`, `verificationStatus`, `classificationCodes`(등록된 `company_classification_codes` 목록, `{id, classificationCode}` 배열), `profileComplete`(업종 1개 이상 AND 지역 1개 이상 등록 시 true)를 반환한다 — 지금까지 `PATCH /companies/me`만 있고 대응하는 읽기 엔드포인트가 하나도 없어 온보딩 화면이 지역 등록값을 재조회할 수 없었던 실제 기능 공백을 메운다. 이 엔드포인트는 이후 플랜(02-06 피드/상세, 02-07 알림설정)이 업체 프로필을 읽어야 할 때 재사용할 수 있는 공용 조회 지점이다.

    `apps/web/lib/classification-tree.data.ts`에 업종-물품분류-매핑.md §목표 업종 매핑표를 그대로 반영한 정적 추천 트리를 만든다: 정보통신·소프트웨어개발(대분류 `43`), 사무용품(`44`), 인쇄·출판(`55`), 시설관리(코드 미확정 — `confirmed: false`로 표시해 UI가 "코드 확인 중" 배지를 렌더링하고 선택 대신 자유 입력 필드로 유도). UNSPSC 전체 42개 세그먼트 매핑은 이번 플랜 범위 밖이다(D-08/§이번 Phase 범위 밖 (b), `업종-물품분류-매핑.md`).

    `apps/web/app/onboarding/page.tsx`(단일 라우트, React 상태로 5스텝 진행 관리, 스텝별 진행 표시 "N/5")에 `ClassificationStep.tsx`(대분류 카드 클릭 시 아코디언으로 중분류 펼침, 다중 체크박스, 상단 "선택됨: N개" 칩)와 `RegionStep.tsx`(시/도 다중 선택)를 연결한다. 업종 미선택 상태로 "다음" 클릭 시 인라인 오류로 진행을 막는다(01-onboarding.md §엣지 케이스). 각 스텝에서 선택이 바뀔 때마다 즉시 API를 호출해 저장한다(뒤로가기 시 로컬 상태로 값 보존, 새로고침 시 `ClassificationStep`은 `GET /companies/me/classification-codes`로, `RegionStep`은 신규 `GET /companies/me`로 각각 재조회한다).
  </action>
  <verify>
    <automated>npm run test:e2e --workspace=apps/api -- classification-codes.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 대분류 2개(`43`,`44`) + 중분류 1개(`4412`)를 등록하면 `GET /companies/me/classification-codes`가 3건을 반환한다
    - 업종을 하나도 선택하지 않고 "다음"을 누르면 프론트에서 API 호출 없이 인라인 오류가 뜬다
    - 다른 업체 소유의 classification-code id로 DELETE 시도 시 403
    - 지역 2개(`서울특별시`,`경기도`) 등록 후 `GET /companies/me`가 `regionCodes: ["서울특별시","경기도"]`와 등록된 `classificationCodes` 목록을 함께 반환한다
  </acceptance_criteria>
  <done>온보딩 스텝 2~3이 다중 업종·지역을 실제로 저장·재조회한다.</done>
</task>

<task type="auto">
  <name>실적·인증(선택) + 알림 초기설정 안내 — 온보딩 스텝 4~5</name>
  <read_first>docs/design/wireframes/01-onboarding.md (스텝 4·5, §데이터 소스)</read_first>
  <files>apps/api/src/companies/performances.controller.ts, apps/api/src/companies/certifications.controller.ts, apps/web/components/onboarding/PerformanceCertStep.tsx, apps/web/components/onboarding/NotificationInitialStep.tsx</files>
  <action>
    `performances.controller.ts`: `POST/GET/DELETE /companies/me/performances`(사업명 필수, 계약금액·계약일자·발주기관 선택 — db-schema-design.md `company_performances`는 `project_name` 외 NOT NULL 없음). `certifications.controller.ts`: `POST/GET/DELETE /companies/me/certifications`(종류 필수, 번호·만료일 선택).

    `PerformanceCertStep.tsx`: 반복 입력 폼(추가/삭제 가능한 행 리스트) + "건너뛰기" 버튼(클릭 시 API 호출 없이 즉시 스텝 5로 이동, 01-onboarding.md §레이아웃 스텝4 "건너뛰기 가능"). `NotificationInitialStep.tsx`: 02-02가 가입 시 이미 만든 기본 `notification_settings`(이메일 on) 요약을 읽기 전용으로 보여주고 "알림 설정 화면으로 이동" 링크(04-notification-settings.md, 02-07이 구현하는 라우트 `/settings/notifications`)를 제공한다 — 이 스텝에서 새 알림 설정 API를 만들지 않는다(그 소유권은 02-07). "완료" 클릭 시 `/feed`로 이동한다.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- performances.controller.spec.ts certifications.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 실적 2건 등록 후 `GET /companies/me/performances`가 2건 반환, 건너뛰기 시 0건이어도 온보딩은 정상 완료된다
    - "완료" 클릭 시 `/feed`로 라우팅된다(02-06에서 실제 피드 화면이 만들어지기 전까지는 임시 placeholder 라우트라도 404 없이 존재해야 한다 — 이 태스크가 `apps/web/app/feed/page.tsx` placeholder를 생성)
  </acceptance_criteria>
  <done>실적·인증 등록/건너뛰기가 동작하고 온보딩 전체 5스텝이 끊김 없이 완주 가능하다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → NestJS API | 업종·지역·실적·인증 CRUD 입력(미신뢰) |
| JWT 소유자 ↔ 리소스 소유자 | 모든 프로필 하위 리소스(classification-codes/performances/certifications)는 `company_id` 소유권 검증 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | classification-codes/performances/certifications DELETE | high | mitigate | 모든 삭제·조회 쿼리에 `WHERE company_id = :jwtCompanyId` 강제 — db-schema-design.md §Phase 2 인계 사항 5(T-01-12)의 직접 구현 지점 |
| T-02-07 | Tampering | classification_code 형식 위조(자릿수·숫자 아님) | medium | mitigate | `class-validator` DTO가 CHECK 제약과 동일한 정규식(길이 2/4/6/8, `^[0-9]+$`)을 API 레벨에서도 검증 |
| T-02-08 | Information Disclosure | 실적 계약금액 등 민감 사업 정보 노출 | low | accept | 소유 업체 본인 조회로만 제한되며 별도 공개 API 없음 |
</threat_model>

<verification>
- `npm run test:e2e --workspace=apps/api -- classification-codes.e2e-spec.ts` 통과
- `npm run test --workspace=apps/api` 전체 통과
- 온보딩 5스텝을 처음부터 끝까지 진행해도 브라우저 콘솔 오류 없이 `/feed`에 도달한다(수동 확인 대상, SUMMARY에 기록)
</verification>

<success_criteria>
PROF-01~04가 실제 CRUD API + 온보딩 UI로 완결되고, 새로고침에도 데이터가 유지된다. `GET /companies/me`가 다운스트림 플랜이 재사용할 수 있는 프로필 조회 지점으로 존재한다.
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-04-SUMMARY.md` when done
</output>
